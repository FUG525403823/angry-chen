# C06 预测与和解
> 里程碑：MC06 ｜ 预估：1.5 人日 ｜ 上游移交物：HANDOFF-C05

## 1. 目标

本步骤结束后，本机陈sir 的移动与弹药显示在丢包、抖动与 RTT 变化下都与服务器权威一致：未确认命令进环形缓冲按 FIFO 重放，权威姿态到达后回滚重放，误差 ≤ 1.0m 走视觉平滑、> 1.0m 硬纠正；弹药开火当帧扣减、同一弹匣周期只降不升、停火 500ms 后与权威静默对齐。跨语言正确性由 `docs/evidence/fixtures/` 的对拍向量逐位验证（ADR-010）。

## 2. 入口条件
**入口条件**：HANDOFF-C05

**跨链前置**：`S06`（局部步进 `localStep` 的移动/碰撞语义，客户端预测与服务端同一份规则）与 `S07`（对拍向量及其导出格式）。二者在 [README](../README.md) 的 order 表中都排在本步之前。

| # | 必须已成立的事实 | 验证命令 | 期望 |
|---|---|---|---|
| 1 | C05 已交付本地预测步进 | `Test-Path client/Assets/Scripts/Sim/Predictor.cs` | `True` |
| 2 | 对拍向量已入库（S07 导出，清单由 `--list` 给出） | `node tools/export-fixtures.mjs --list` | 14 行场景名（不含 `trig-table.json`） |
| 3 | 团结引擎编辑器可用（`$env:AC_UNITY`，2022.3.62t16） | `Test-Path $env:AC_UNITY` | `True` |
| 4 | 确定性运算子集可指认 | `Select-String -Path ../../00-共识/ADR/ADR-010-跨语言确定性与对拍.md -Pattern '超越函数'` | 命中 ADR-010 §决策 3 |
| 5 | 文档与素材门禁可用 | `node tools/check-docs.mjs` 与 `node tools/check-assets.mjs` | 退出码 0 |

## 3. 交付物

| 文件 | 状态 | 职责 |
|---|---|---|
| `client/Assets/Scripts/Sim/CommandBuffer.cs` | 新建 | 未确认命令环形缓冲、`ushort` 回绕比较、ack 裁剪 |
| `client/Assets/Scripts/Sim/Reconciler.cs` | 新建 | 权威对账：回滚、按序重放、误差判定与平滑/硬纠正的判定结果（偏移量交 View 层施加） |
| `client/Assets/Scripts/Sim/AmmoLedger.cs` | 新建 | 弹药本地账：当帧扣弹、只降不升、停火自愈、开火闸门 |
| `client/Assets/Tests/reconcile_test.cs` | 新建 | 缓冲/重放/误差/账本规则的单测 |
| `client/Assets/Tests/fixture_predict_test.cs` | 新建 | 加载 ADR-010 fixture 并逐位复现 |

## 4. 任务清单

- [ ] 1. 写 `client/Assets/Scripts/Sim/CommandBuffer.cs`：构造时一次分配 128 槽（`Command` 结构体数组），实现 `Push` / `AckUpTo` / `At` / `Size` / `OverflowCount` 与 `SeqDiff`；满时丢最旧。
- [ ] 2. 写 `client/Assets/Scripts/Sim/Reconciler.cs`：`Reconcile(in LocalAuthority, CommandBuffer, Predictor) → ReconcileResult { replayed, errM, offsetX/offsetY/offsetZ, hardCorrect }`；先落地权威姿态、再 `AckUpTo(authority.lastAckedSeq)`、再从最旧到最新逐条 `localStep(50ms)`；`Ac.Sim` 不得引用 `Ac.View`（C01 §5.2），平滑不在本文件里做。
- [ ] 3. 在 `Reconciler.cs` 内实现误差判定：`err = sqrt(dx*dx+dy*dy+dz*dz)`（`double`）；`err <= 1.0m` 时把差值交给 **View 层**的 `client/Assets/Scripts/View/ErrorSmoother.cs`（C04 交付，只作用于渲染变换），`err > 1.0m` 时硬纠正并置 `hardCorrect`，由 View 层 `ErrorSmoother.Reset()`。
- [ ] 4. 写 `client/Assets/Scripts/Sim/AmmoLedger.cs`：实现 §5(c) 的四条规则（当帧扣弹、只降不升、停火自愈、周期重置）与 `GateMag = min(optimistic, mag)`；账本行用定长数组存储，零分配。
- [ ] 5. 写 `client/Assets/Tests/reconcile_test.cs`：覆盖回绕（`65535 → 0`）、溢出丢最旧、重放顺序、`err = 1.0` 边界、0.65924/tick 衰减、0.001m 归零、账本三规则与开火闸门。
- [ ] 6. 写 `client/Assets/Tests/fixture_predict_test.cs`：**不遍历** `docs/evidence/fixtures/*.json`（会命中 `trig-table.json`），改按 `node tools/export-fixtures.mjs --list` 的 14 份清单逐份加载，按 `ticks` 逐条 `Push` 与 `AckUpTo` 后重放，用 `BitConverter.DoubleToInt64Bits` 逐位比较 `pos/yaw/pitch` 与 `rngState`，首个差异打印 fixture 名、tick、字段路径与两侧位模式；两份测试的用例都注册进 `Ac.Tests.SuiteRegistry.RunAll`（`reconcile.*` / `fixture_predict.*`）。
- [ ] 7. 把 `LastErrorM` / `MaxErrorM` / `HardCorrectCount` / `RejectedTotal` 接进调试日志行（调试面板留给后续步骤），供 §6 人工核对。

## 5. 冻结契约

冻结的 v1 对照实现：`packages/client/src/prediction/reconciler.ts` 与 `packages/client/src/prediction/ammoLedger.ts`（只作对照，不修改）。

**(a) 未确认命令缓冲**

| 项 | 值 / 定义 |
|---|---|
| 容量 | `CommandBufferCapacity = 128` 条 |
| 序号 | `ushort`，回绕模 65536；`SeqDiff(a,b) = (a-b)&0xffff`，≥ `32768` 时减 65536（> 0 表示 a 更新） |
| 溢出规则 | 满 128 条时丢弃**最旧**一条并 `OverflowCount += 1` |
| 过期规则 | 只由权威 `lastAckedSeq` 推进（`AckUpTo`）；缓冲不自行过期 |
| 裁剪规则 | 丢弃所有满足 `SeqDiff(seq, slot.Seq) >= 0` 的最旧条目，返回丢弃条数 |
| 分配 | 构造期一次性分配；`Push` / `AckUpTo` 零托管分配 |

**(b) 回滚重放与误差**

| 项 | 值 / 定义 |
|---|---|
| `HardCorrectThresholdM` | `1.0` |
| 误差定义 | `err = sqrt(dx*dx+dy*dy+dz*dz)`，(dx,dy,dz) = 和解前位置 − 重放后位置，全程 `double` |
| 重放顺序 | 落地权威姿态 → `AckUpTo` → 从**最旧到最新**逐条 `localStep(50ms)` |
| 平滑时间常数 | `SmoothingTimeConstantS = 0.12` |
| 每 tick 衰减 | `SmoothingDecayPerTick = 0.65924`（= e^(−0.05/0.12) 的固定步长形式，禁 `exp`，ADR-010） |
| 归零阈值 | `SmoothingEpsilonM = 0.001`，模长小于此值直接归零 |
| 切换规则 | `err <= 1.0` → 平滑（差值交 View 层施加，见下行）；`err > 1.0` → 硬纠正 + `HardCorrectCount += 1`，View 层同时 `ErrorSmoother.Reset()` |
| 平滑器归属 | 唯一实现是 C04 的 `client/Assets/Scripts/View/ErrorSmoother.cs`（View 层）；`Ac.Sim` 不得引用 `Ac.View`（C01 §5.2），本步的 Sim 层只输出误差与偏移，平滑由 View 层施加。原 `SmoothingOffsetCapM = 0.5` 与 C04 的冻结判据冲突（C04 只有 `e <= 0.001m` 忽略、`0.001m < e <= 1.0m` 交 `ErrorSmoother`、`e > 1.0m` 立即吸附三档，没有偏移上限），**已删除该常量** |
| 作用域 | 平滑只作用于渲染变换，绝不写回模拟状态 |

**(c) 弹药本地账**

| 项 | 值 / 定义 |
|---|---|
| 槽位数 | `AmmoSlotCount = 3` |
| 当帧扣弹 | `mag = max(0, serverMag − pending)`，本地开火当帧数字就掉 |
| 只降不升 | 同一弹匣周期内 `mag` 单调不升（`pending` 减少也不回升） |
| 停火自愈 | `pending == 0` 且距最后一次本地开火 ≥ `AmmoIdleHealMs = 500` 时 `mag` 静默回到权威值 |
| 周期重置 | 权威弹匣上升（换弹/补弹）或切换槽位 → `shownMag = optimistic` |
| 开火闸门 | `GateMag = min(optimistic, mag)`；`GateMag == 0` 时本地不产生曳光 |
| 过期判定 | `AckedSeq − AmmoAckGraceSeq > seq`（`AmmoAckGraceSeq = 20`）仍无对应权威下降 → 计入 `RejectedTotal`，且不回弹显示值 |
| 消账顺序 | 权威每下降 1 发消掉**最早**的一条未确认开火（FIFO） |

**(d) 与权威对账的字段来源**

| 对账量 | 来源字段 |
|---|---|
| 本机位置/朝向 | 快照本机实体 `SnapshotEntity.x/y/z/yaw/pitch`，由 C04 投影为 `LocalAuthority { found, x, y, z, yaw, pitch, lastAckedSeq, tick }` |
| 已确认序号 | `LocalAuthority.lastAckedSeq` |
| 弹匣/备弹 | `MatchStatePlayer.mag` / `MatchStatePlayer.reserve`（由 S10 的 `MatchState` 单播提供） |
| 当前槽位 | `MatchStatePlayer.weapon`（由 S10 的 `MatchState` 单播提供） |
| 对拍向量 | `docs/evidence/fixtures/` 的 14 份 fixture，清单由 `node tools/export-fixtures.mjs --list` 给出（`trig-table.json` 不是对拍向量，不得被遍历），字段 `seed / configHash / ticks / expected`（ADR-010 §决策 6） |

`MatchStatePlayer.*` / `MatchState.*` 的 `mag` / `reserve` / `rage` / `rageLeft100Ms` / `reloadLeft10Ms` / `reviveRatio255` / `phase` / `wave` / `intermissionMs` / `aliveMs` 与 S10 §5 的字段逐名一致，**全部由 S10 的 `MatchState` 单播提供**（本步只读，不走快照通道）。

**(e) 对拍比较口径**

| 项 | 值 / 定义 |
|---|---|
| 浮点比较 | `BitConverter.DoubleToInt64Bits` 逐位相等，不设容差 |
| 失败输出 | fixture 名 + tick + 字段路径 + 两侧位模式，随后断言失败 |
| 覆盖下限 | 至少覆盖静止、直线移动、碰撞、连射命中四类 fixture |

## 6. 验证

| # | 命令 | 期望 | 失败意味着 |
|---|---|---|---|
| 1 | `& $env:AC_UNITY -batchmode -quit -nographics -projectPath client -executeMethod Ac.Tests.SuiteRegistry.RunAll -logFile -` | 末行 `SELFTEST OK` 且全输出无 `FAIL` | 回滚重放顺序或误差判据写错，预测与 S06 语义不一致 |
| 2 | 上条批处理命令的输出 | 含 `PASS reconcile.` 与 `PASS fixture_predict.` 前缀的用例各 ≥ 4 条 | 用例未注册进 `Ac.Tests.SuiteRegistry.RunAll`，或 fixture 未加载（路径或 JSON 字段名与 ADR-010 不符） |
| 3 | `node tools/export-fixtures.mjs --check` | `14/14 identical`（清单由 `--list` 给出，不含 `trig-table.json`） | fixture 被手工改动或不是 ADR-010 格式，S07 移交物不完整 |
| 4 | `Select-String -Path client/Assets/Scripts/Sim/AmmoLedger.cs -Pattern 'AmmoIdleHealMs'` | 命中 `500` | 停火自愈窗口被改，弹药显示会长期偏离权威 |
| 5 | `node tools/check-docs.mjs` 与 `node tools/check-assets.mjs` | 退出码 0 | 本份文档被门禁拒绝，或有人把二进制素材放进 `client/` |

## 7. DoD（验收标准）

- [ ] 缓冲容量 128、溢出丢最旧、`ushort` 回绕比较正确（`reconcile_test.cs` 有用例）
- [ ] 误差 `> 1.0m` 触发硬纠正、`<= 1.0m` 走平滑（0.65924/tick、0.001m 归零）各有断言
- [ ] 弹药账三规则（当帧扣弹 / 只降不升 / 停火 500ms 自愈）与 `GateMag = 0` 不开火各有断言
- [ ] `fixture_predict_test.cs` 按 `--list` 清单覆盖全部 14 份对拍向量（不含 `trig-table.json`）且逐位一致，首个差异可定位到字段路径
- [ ] 分组自检 `Ac.Tests.SuiteRegistry.RunAll` 无 `FAIL`、末行 `SELFTEST OK`；`node tools/check-docs.mjs`、`node tools/check-assets.mjs` 退出码 0
- [ ] 新增文件全部为纯文本 `.cs`，仓库无二进制素材、无第三方包

## 8. 风险与回滚

| 风险 | 触发信号 | 对策 |
|---|---|---|
| 跨语言浮点不收敛 | `fixture_predict_test` 首个差异出现在某坐标最低位 | 先核对 ADR-010 子集（禁 `MathF` / `exp` / 超越函数），再确认两侧 fixture 版本一致；两次会话仍不收敛则按 ADR-008 回退到"纯插值不预测" |
| 平滑掩盖真实发散 | `MaxErrorM` 连续 60 帧 > 0.3m | 暴露 `LastErrorM/MaxErrorM`，超阈值打结构化日志并采样 1 帧命令缓冲 |
| 弹药账长期偏离权威 | `RejectedTotal` 增速 > 1/分钟 | 复核 `AmmoAckGraceSeq = 20` 与快照率（10–30Hz）的匹配；必要时把只降不升窗口收敛到一个弹匣周期 |
| 和解拖慢帧时间 | 单帧和解耗时 P95 > 1ms | 缓冲上限 128 已封顶；重放只在收到权威快照时发生 |

**回滚目标**：回到 `HANDOFF-C05` 状态（保留本地预测与输入上行），删除 `CommandBuffer.cs` / `Reconciler.cs` / `AmmoLedger.cs` 与两份测试；或让 `Reconcile` 在 `found == false` 时直接返回，不修正、不平滑。

## 9. 移交物
**移交物 ID**：HANDOFF-C06

稳定接口清单：

- `CommandBuffer`：`Push(Command)` / `AckUpTo(ushort)` / `At(int)` / `Size` / `OverflowCount`
- `Reconciler`：`Reconcile(in LocalAuthority, CommandBuffer, Predictor) → ReconcileResult`（`replayed` / `errM` / 偏移三分量 / `hardCorrect`）、`HardCorrectCount` / `LastErrorM` / `MaxErrorM` / `Reset()`；平滑器不在本层（见 §5(b) 平滑器归属）
- `AmmoLedger`：`NoteLocalShot(seq, slot, nowMs)` / `NoteServerAck(seq)` / `Reconcile(serverMag, serverReserve, slot, nowMs) → AmmoView` / `RejectedTotal` / `Reset()`

已验证能力清单：见 §7（缓冲与回绕、回滚重放、误差切换、弹药账三规则、fixture 逐位复现）。
