# S13 持久化、日志、指标与诊断报告
> 里程碑：MS13 ｜ 预估：1 人日 ｜ 上游移交物：HANDOFF-S12

## 1. 目标

本步结束后，一局对局能留下三份可离线消费的证据——追加式战绩（NDJSON，带可替换的存储缝）、结构化 JSON 行日志、单局诊断报告——并且进程自身暴露 `/health`、`/metrics` 与两个读接口；运维不再需要读源码就知道"这一局抖了多少、发了多少字节、有多少异常命令"。字段与端点口径继承 v1 冻结对照实现 `match_store.ts`、`report.ts`、`http.ts`、`log.ts`、`metrics.ts` 与 `O09 战绩存储与HTTP层`。

## 2. 入口条件

**入口条件**：HANDOFF-S12

跨链前置：无（服务器链不依赖客户端链，可独立推进到链尾）。

| # | 必须已成立的事实 | 验证命令 | 期望 |
|---|---|---|---|
| 1 | 复制流水线的字节与队列账本可用（快照字节、丢弃、档位） | `server/build/ac_tests.exe --filter=replication` | 全绿 |
| 2 | tick 抖动、工作量与漂移采样可用 | `server/build/ac_tests.exe --filter=schedule` | 全绿 |
| 3 | 对局流程能跑到 `ended` 并产出结算数据（击杀/救援/波次） | `server/build/ac_tests.exe --filter=match` | 全绿 |
| 4 | 异常命令计数可用（`malformed` / `rateLimited` / `dropped`） | `server/build/ac_tests.exe --filter=security` | 全绿 |
| 5 | 构建入口可复现 | `cmake --build server/build -j` | 退出码 0 |

## 3. 交付物

| 文件 | 状态 | 职责 |
|---|---|---|
| `server/src/persist/match_store.hpp` | 【新建】 | `MatchStore` 抽象接口与 `MatchResultRecord` 结构（sqlite 适配器缝） |
| `server/src/persist/match_store.cpp` | 【新建】 | `<dataDir>/matches.ndjson` 的 NDJSON 追加实现：流式加载、常驻上限批量淘汰、top/recent 查询缓存 |
| `server/src/metrics/metrics.cpp` | 【新建】 | 计数/仪表注册表与 Prometheus 文本渲染（指标名清单见 §5） |
| `server/src/report.cpp` | 【新建】 | 单局诊断报告聚合与落盘（`reports/` 保留最近 200 份） |
| `server/src/http/server.cpp` | 【新建】 | `/health`、`/metrics`、`/api/leaderboard`、`/api/matches/recent` 与状态码 |
| `server/tests/store_test.cpp`、`server/tests/http_test.cpp` | 【新建】 | 战绩追加/重载/淘汰/排序一致；四个端点状态码、JSON 形状与读限流 429 |

## 4. 任务清单

- [ ] 1. 定义 `server/src/persist/match_store.hpp`：`MatchStore` 接口（`append` / `listTop` / `listRecent` / `flush` / `version` / `recordCount`）与 `MatchResultRecord` 字段。
- [ ] 2. 实现 `server/src/persist/match_store.cpp`：`<dataDir>/matches.ndjson`（`AC_DATA_DIR` 下，文件名固定）追加写、流式加载（逐行解析，坏行只计数）、常驻上限 10000 与批量淘汰。
- [ ] 3. 在 `match_store.hpp` 预留 `SqliteMatchStore` 适配器缝（同一接口 + `Open(path)` 工厂声明 + 编译期开关 `AC_WITH_SQLITE=0`），本步不实现。
- [ ] 4. 实现 `server/src/metrics/metrics.cpp`：`/metrics` 渲染为 `text/plain; version=0.0.4`，指标名清单按 §5 冻结。
- [ ] 5. 实现 `server/src/report.cpp`：对局结束时写 `reports/<matchId>.json`，按 mtime 保留最近 200 份。
- [ ] 6. 实现 `server/src/http/server.cpp`：四个端点 + 读限流（30 次/分钟/IP，超限 429 与 `retry-after: 60`）+ 60s TTL 读缓存。
- [ ] 7. 编写 `server/tests/store_test.cpp`：含 10 万行加载用例（常驻条数不随行数增长）与"查询与全排序取前 N 逐条一致"。
- [ ] 8. 编写 `server/tests/http_test.cpp`：覆盖 200/404/429/500 与 JSON 字段名；更新 `server/README.md` 运维段（三条落盘路径、四个端点、指标速查表入口）。

## 5. 冻结契约

战绩记录 schema（`match_store.hpp`，NDJSON 一行一条，字段名即 JSON 键）：

| 字段 | 类型 | 语义 |
|---|---|---|
| `matchId` | string | 对局唯一 id（文件名安全字符集 `[A-Za-z0-9._-]`） |
| `startedAtMs` | u64 | 对局开始的 Unix 毫秒 |
| `durationMs` | u32 | 对局时长（毫秒） |
| `waveReached` | u16 | 到达的最高波次 |
| `winnerTeam` | u8 | 0 或 1 |
| `playerCount` | u8 | 参战人数（1…4） |
| `players[]` | array | `{ name, kills, headshots, shotsFired, hits, revives, downs, aliveMs, leftMidMatch }` |

| 冻结项 | 值 / 定义 |
|---|---|
| 存储文件 | `<dataDir>/matches.ndjson`（战绩，只追加，永不截断）；`<dataDir>/reports/<matchId>.json`（单局诊断报告，保留最近 200 份）；`<dataDir>` 即 `AC_DATA_DIR` |
| 常驻上限 | `kMaxRecords = 10000`；超出时批量淘汰 `max(1, kMaxRecords/100)` 条最旧记录 |
| 坏行处理 | 解析失败或字段缺失 → 跳过并计 `ac_corrupt_lines_total`，不抛异常 |
| 排序一致性 | top = 击杀降序 → `startedAtMs` 降序 → `matchId` 升序决胜；recent = `startedAtMs` 降序 → `matchId` 升序 |
| sqlite 缝 | 同一接口的实现声明 `SqliteMatchStore` 保留；`AC_WITH_SQLITE` 默认 0，启用需先改 ADR |

日志（JSON 行，UTF-8，每行一个对象）：

| 冻结项 | 值 |
|---|---|
| 字段 | `{ ts, level, evt, room, tick, pid, detail }`；`detail` 为扁平键值（string/number/bool/null/string[]） |
| 级别 | `debug`=10、`info`=20、`warn`=30、`error`=40；默认 `info`，由 `AC_LOG_LEVEL` 覆盖 |
| 事件名（最小集） | `room.create`、`room.reclaim`、`session.join`、`session.leave`、`session.rate_limited`、`grace.start`、`grace.reconnect`、`grace.timeout`、`match.start`、`match.end`、`wave.start`、`wave.clear`、`anticheat.speed`、`store.error`、`report.write_failed`、`listening`、`shutdownRequested`、`shutdownComplete`、`error.uncaught` |
| 频率纪律 | 每 tick 不打印常态日志；未捕获异常必须落 `error.uncaught` 且进程保持可用 |

`/metrics` 指标名清单（本步冻结；新增名需同步本表）：

| 类别 | 名称 |
|---|---|
| 版本 | `ac_server_version`（标签 `version`/`protocol`/`tick_ms`，与 `--version` 行 `ac_server 0.1.0 protocol=1 tick=50ms` 同源） |
| 调度 | `ac_tick_schedule_error_ms_p95`、`ac_tick_interval_error_ms_p95`、`ac_tick_jitter_ms_p50`、`ac_tick_jitter_ms_p95`、`ac_tick_work_ms_p95`、`ac_tick_work_ms_p99`、`ac_sim_drift_ms`、`ac_room_budget_exceeded_total` |
| 复制与背压 | `ac_snapshots_sent_total`、`ac_snapshot_bytes_total`、`ac_snapshot_bytes_avg`、`ac_snapshot_bytes_max`、`ac_snapshot_records_avg`、`ac_snapshot_rate_x10`、`ac_snapshot_rate_downshifts_total`、`ac_send_queue_bytes`、`ac_slow_client_drops_total` |
| 校验 | `ac_malformed_frames_total`、`ac_oversized_frames_total`、`ac_rate_limited_frames_total`、`ac_dropped_frames_total`、`ac_pose_suspect_total`、`ac_pose_rejected_total`、`ac_hard_correct_total`、`ac_rewind_clamped_total`、`ac_speed_violations_total` |
| 会话与房间 | `ac_rooms`、`ac_connections`、`ac_players`、`ac_grace_active`、`ac_grace_starts_total`、`ac_grace_reconnects_total`、`ac_grace_timeouts_total` |
| 流量与存储 | `ac_bytes_out_total`、`ac_bytes_in_total`、`ac_frames_out_total`、`ac_frames_in_total`、`ac_events_sent_total`、`ac_events_dropped_total`、`ac_records_retained`、`ac_corrupt_lines_total`、`ac_http_rate_limited_total`、`ac_http_cache_hits_total`、`ac_uptime_seconds` |

单局诊断报告 `reports/<matchId>.json`：

| 字段 | 语义 |
|---|---|
| `matchId` / `durationMs` | 对局标识与时长 |
| `ticks.total` / `ticks.skipped` | 执行 tick 数与跳过的 tick 数 |
| `ticks.jitterMsP50` / `ticks.jitterMsP95` | tick 抖动（口径同 S12 §5 的 tick 抖动定义）P50/P95，保留 3 位小数 |
| `ticks.scheduleErrorMsP95` / `ticks.workMsP95` / `ticks.workMsP99` / `ticks.simDriftMsMax` | 调度误差与工作量、漂移 |
| `net.snapshotBytesAvg` / `net.snapshotBytesMax` / `net.bytesOutTotal` / `net.messagesInTotal` / `net.malformedInTotal` / `net.rateLimitedTotal` | 出站字节（快照均值、单帧最大、总出站）与异常命令计数（入站总数、畸形、限流） |
| `fair.*` | `hardCorrectTotal`、`poseSuspectTotal`、`poseRejectedTotal`、`rewindClampedTotal`、`speedViolationsTotal`、`shotsFiredTotal`、`hitsTotal` |
| `grace.*` / `peak.*` | 宽限期开始/重连/超时次数；实体与人数峰值 |

HTTP 端点与状态码：

| 方法与路径 | 参数 | 状态码 | 响应 |
|---|---|---|---|
| `GET /health` | — | 200 | `{ status:"ok", protocolVersion, rooms, connections, players, ticks }` |
| `GET /metrics` | — | 200 | `text/plain; version=0.0.4` |
| `GET /api/leaderboard`、`GET /api/matches/recent` | `limit`（默认 20 / 10，上限 100） | 200 / 429 / 500 | `{ ok:true, entries:[...] }`；429 带 `retry-after: 60` |
| 其它路径 | — | 404 | `{ error:"not-found", path }` |
| 存储尚未加载完成 | — | 503 | `{ ok:false, error:"store-not-ready" }` |

读限流与缓存：`30 次/分钟/IP`（滑动窗口）、命中限流计 `ac_http_rate_limited_total`；读缓存 TTL `60s`、键 `path:limit`、写入新对局或触发淘汰即失效，命中计 `ac_http_cache_hits_total`。

## 6. 验证

| # | 命令 | 期望 | 失败意味着 |
|---|---|---|---|
| 1 | `server/build/ac_tests.exe --filter=store`；`server/build/ac_tests.exe --filter=http` | 两套件全绿 | schema、淘汰或状态码与 §5 不一致 |
| 2 | `server/build/ac_tests.exe --filter=store --fixture build/ndjson-100k.ndjson` | 输出 `retained=10000 corrupt=0` 且常驻条数不随行数增长 | 仍是整文件读入，或淘汰未生效 |
| 3 | `(Invoke-WebRequest -UseBasicParsing http://localhost:8787/health).StatusCode`；`((Invoke-WebRequest -UseBasicParsing http://localhost:8787/metrics).Content \| Select-String -Pattern "^ac_" \| Measure-Object).Count` | 前者 `200`（响应含 `protocolVersion`）；后者 ≥ 41（含版本行 `ac_server_version`） | 端点未接线或指标名漂移（清单被改而未同步 §5） |
| 4 | `1..31 \| ForEach-Object { try { (Invoke-WebRequest -UseBasicParsing "http://localhost:8787/api/leaderboard?limit=5").StatusCode } catch { $_.Exception.Response.StatusCode.value__ } }` | 第 31 次为 `429` | 读限流未生效 |
| 5 | `(Get-ChildItem <dataDir>/reports/*.json).Count`；`Get-Content <dataDir>/matches.ndjson -TotalCount 1 \| ConvertFrom-Json` | 报告数 ≤ 200；NDJSON 首行是合法 JSON 且含 `matchId` | 保留策略或追加写失败 |
| 6 | `server/build/ac_server.exe --selftest-log \| ConvertFrom-Json`；再以 `AC_LOG_LEVEL=warn` 复跑 | 打出含 `ts`/`level`/`evt` 的合法 JSON 对象；`debug` 行不落盘 | NFR-09 的日志面失效（不再是结构化 JSON 行） |
| 7 | `node tools/check-docs.mjs` | 退出码 0 | 文档门被本步新链接或章节破坏 |

人工复核（一次）：`Select-String -Path server/src/report.cpp,server/src/metrics/metrics.cpp -Pattern "std::(log|sin|cos|pow|exp)\("` 应无命中（日志与指标不得进入模拟路径）。

## 7. DoD（验收标准）

- [ ] `server/build/ac_tests.exe` 全绿（`--filter=store`、`--filter=http` 两组）。
- [ ] 10 万行 NDJSON 加载后常驻记录 ≤ 10000，坏行只计数；查询结果与全排序取前 N 逐条一致。
- [ ] `/health`、`/metrics`、两个读接口四个端点状态码符合 §5；读限流第 31 次返回 429。
- [ ] §5 指标名清单的名字在 `/metrics` 全部可取；单局诊断报告 8 组字段齐全且 `jitterMsP50 < jitterMsP95`、`snapshotBytesMax ≥ snapshotBytesAvg`。
- [ ] 日志为 JSON 行且字段名与 §5 一致；未捕获异常落 `error.uncaught` 后进程仍可用。
- [ ] sqlite 适配器缝存在（接口 + 工厂声明 + 开关），且默认关闭。
- [ ] `node tools/check-docs.mjs`、`node tools/check-assets.mjs` 退出码 0。

## 8. 风险与回滚

| 风险 | 触发信号 | 对策 |
|---|---|---|
| 写盘路径影响稳态或无限增长 | `ac_tick_work_ms_p99` 在同局结束时刻出现同步尖峰，或 `ls reports/ | wc -l` 持续上升 | 写盘走独立队列、对局结束批量 flush；报告按 mtime 保留 200 份，清理失败只记日志不抛异常 |
| 读缓存导致战绩"看不到新对局" | 新对局结束 60s 内查询命中旧缓存 | 缓存键带 `version`（写入/淘汰即失效），测试断言写入后立即可见 |
| 指标名漂移打断既有看板 | `/metrics` 上某名字消失 | §5 清单即契约；改名必须同提交更新清单与断言；旧名保留为别名至少一个里程碑 |
| 回滚目标 | — | `git revert` 本步提交：退回"仅控制台日志、无战绩/无指标/无报告"的 S12 行为；对局功能不受影响 |

## 9. 移交物

**移交物 ID**：HANDOFF-S13

稳定接口：

- `MatchStore` 接口与 `MatchResultRecord` 字段集合（含 `version` / `recordCount` 读口）；`SqliteMatchStore` 缝。
- `render_metrics() -> std::string`：Prometheus 文本渲染口，指标名清单即契约。
- `build_match_diagnostics(...) -> MatchDiagnostics` 与 `write_report(dataDir, diagnostics) -> path`。
- `handle_http(request) -> response`：四个端点与状态码表。
- 日志写入器 `log(level, evt, fields)` 与事件名最小集。

已验证能力：

- 战绩追加、流式加载、批量淘汰与查询一致性（含 10 万行用例）。
- 四个端点状态码、读限流 429、60s 缓存失效。
- 单局诊断报告字段齐全且被 `/metrics` 与报告两侧一致引用。

