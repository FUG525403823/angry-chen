# O05 快照编码增量化与自适应快照率

> 里程碑：O5 ｜ 预估：1 会话 ｜ 上游移交物：HANDOFF-O04

## 0. 现状与不足（证据）

本步处理**快照通道**：编码/解码里各有一段 1024 槽的全量扫描，截断路径有两次全排序，而"自适应快照率"这个
已经冻结进协议的字段（`snapshotRateX10`）从头到尾是个常量。

### 0.1 结论一：编码与解码各有一次 1024 槽全量扫描

| 事实 | 证据 |
|---|---|
| `encodeSnapshot` 的删除列表遍历 `for (let id = 1; id <= MAX_ENTITIES; id += 1)`，逐 id 查两个 `Uint8Array` | `packages/shared/src/net/codec.ts:600-614` |
| `MAX_ENTITIES = 1024` | `packages/shared/src/config/entity.ts:2` |
| `decodeSnapshot` 在基线全量帧与每帧末尾各重建一次 `mirror.ids`（同样是 1024 全扫 + `push`） | `packages/shared/src/net/codec.ts:640-646,660-666` |
| `mirror.ids` 是普通 `number[]`（`push` 会增长/搬迁） | `packages/shared/src/net/codec.ts:445-456` |
| 规模 | 4 会话 × 20Hz = 每秒 8.2 万次纯空转迭代（无实体死亡时全部浪费） |

### 0.2 结论二：快照截断路径两次全排序 + 一个对象分配

| 事实 | 证据 |
|---|---|
| `truncated` 时先 `collectNearest`（全量 `push` + 按距离 `sort` + 按 id 再 `sort`） | `packages/shared/src/snapshot.ts:100-116` |
| `playersCentroid` 每次返回 `{x, z}` 对象字面量 | `packages/shared/src/snapshot.ts:81-98` |
| 复用同一个 `world.scratchEntities`（共享可变数组） | `packages/shared/src/snapshot.ts:100-112` |
| 触发条件 | 活动实体数 > `NET.snapshotMaxEntities = 256`；满编 60 羊时不会触发，但 10 波后的召唤潮会 |

### 0.3 结论三：`snapshotRateX10` 是死配置

| 事实 | 证据 |
|---|---|
| 常量 `SNAPSHOT_RATE_X10 = 200` | `packages/shared/src/net/protocol.ts:135,154-158` |
| 房间初始化即固定 200，全生命周期不变 | `packages/server/src/room.ts:90,130,248` |
| 只经 pong 下发（`encodePong` 第 9 字节） | `packages/shared/src/net/codec.ts:390-397` |
| 客户端只存不用于插值 | `packages/client/src/net/connection.ts:170,281,359` |
| 快照无条件每 tick 发送（仅"每 40 tick 一次全量"这一条策略） | `packages/server/src/room.ts:483-500,513-530`、`packages/shared/src/net/protocol.ts:127` |

### 0.4 结论四：客户端插值延迟是固定 100ms

| 事实 | 证据 |
|---|---|
| `INTERPOLATION_DELAY_MS = 100`，与快照率、RTT、抖动全无关系 | `packages/client/src/net/state.ts:16` |
| 插值窗口 `SNAPSHOT_HISTORY = 6` 个关键帧 | `packages/client/src/net/state.ts:17,107` |
| 服务器时间外推：`latestServerTimeMs + (now() - latestRecvAtMs)` | `packages/client/src/net/state.ts:257` |
| 影响 | 丢包/抖动时 100ms 缓冲不足（证据里 `snapshotArrivalJitterP95Ms = 20–27ms`），而 20Hz 下 100ms 又偏大（相当于 2 帧） |

### 0.5 结论五：带宽现状（说明优化目标不是"省字节"）

证据文件记录快照均值 88.2B、最大 482B、每客户端带宽 1.81KB/s，门槛是 1200B / 40KB/s——
所以本步的目标是**CPU 与自适应能力**，不是压缩字节；协议字节格式必须保持不变。

## 1. 目标

本步骤结束后：

1. 编码删除列表与解码 `mirror.ids` 的维护量从 O(1024) 降到 O(本帧增删数)，**线上字节格式完全不变**；
2. 截断路径从"两次全排序"降到"一次部分选择 + 对小结果集排序"；
3. `snapshotRateX10` 从常量变成**受控的离散档位**（默认仍 20Hz，只在拥塞时降档），并真正用于客户端插值延迟；
4. 客户端插值延迟动态化，取值有界、可解释。

判据：`node tools/bots.mjs --players 4 --minutes 5 --strict` 的 S5.2-1/2/3（字节均值、带宽、19–21/s 速率）与其余门槛全部 PASS，
且 `snapshot-codec.test.ts` 的既有字节断言（不修改）全部通过。

## 2. 入口条件

**入口条件**：HANDOFF-O04

| # | 必须已成立的事实 | 验证命令 | 期望 |
|---|---|---|---|
| 1 | 上一步交付可用 | `pnpm check` | 退出码 0 |
| 2 | 单 tick 预算与基准存在 | `node tools/bench-sim.mjs --sheep 60 --ticks 2000` | 输出 p95/p99 |
| 3 | 出站所有权与背压已落地 | `grep -n "bufferedAmount" packages/server/src/transport/types.ts` | 命中 |
| 4 | 当前速率门槛值可查 | `grep -n "19–21" tools/bots.mjs` | 命中 S5.2-3 |
| 5 | 解码侧消费点可定位 | `grep -n "mirror.ids" packages/shared/src packages/client/src` | 命中 `codec.ts` 与 `state.ts` |

## 3. 交付物

| 文件 | 状态 | 职责 |
|---|---|---|
| `packages/shared/src/net/codec.ts` | 修改 | `SnapshotBaseline` 与 `SnapshotMirror` 增加紧凑 id 列表；删除列表只扫紧凑列表；`mirror.ids` 改 `Uint16Array` + 计数 |
| `packages/shared/src/snapshot.ts` | 修改 | 截断路径部分选择；`playersCentroid` 写 scratch 不返回对象 |
| `packages/server/src/room.ts` | 修改 | 自适应快照率（离散档位 + 升降条件 + 计数指标） |
| `packages/server/src/metrics.ts` | 修改 | `snapshotRateX10`（gauge）、`snapshotRateDownshifts`（counter） |
| `packages/client/src/net/state.ts` | 修改 | 插值延迟动态化（快照间隔中位数 + `snapshotRateX10` 兜底，上限 250ms） |
| `packages/client/src/net/connection.ts` | 修改 | 把 `lastServerSnapshotRateX10` 传给 `SnapshotView` |
| `packages/shared/src/net/snapshot-codec.test.ts` | 修改 | 删除列表升序/只含实际删除项；紧凑列表增删不变量；字节与旧实现一致 |
| `packages/shared/src/snapshot.test.ts` | 修改 | 截断路径选择结果与旧实现一致 |
| `packages/client/src/net/state.test.ts` | 修改 | 插值延迟自适应与上限用例 |
| `packages/server/src/replication.test.ts` | 修改 | 降档/升档条件与速率离散化 |
| `docs/evidence/bots-o05.json` | 新建 | 默认档位下 5 分钟压测证据 |
| `docs/验收报告.md` | 修改 | 回填 §3.2 的 S5.2-1/2/3 复核与新的速率能力 |

## 4. 任务清单

- [x] 1. `codec.ts`：`SnapshotBaseline` 增加 `readonly presentIds: Uint16Array` 与 `presentCount: number`（升序紧凑列表）。`encodeSnapshot` 写入新实体时在列表尾部插入并保持升序；删除列表改为遍历 `presentIds[0..presentCount)`；输出顺序保持**升序**（与旧实现逐字节一致）。
- [x] 2. `codec.ts`：`SnapshotMirror.ids` 改为 `Uint16Array` + `idCount`；解码时对"新增 id"插入升序、对"删除 id"做有序移除；`baselineTick === 0` 时清空。删除每帧末尾的 1024 全扫。
- [x] 3. `codec.ts`：新增两个内部断言函数（仅测试用导出）：`baselinePresentIdsSorted(baseline)`、`mirrorIdsSorted(mirror)`，供测试断言不变量。
- [x] 4. `snapshot.ts`：`collectNearest` 改为"有界最大堆部分选择"（容量 = `capacity`），选出后再按 id 升序排一次（只排 N 个）；`playersCentroid` 写入模块级 `centroidScratch`。
- [x] 5. `room.ts`：`snapshotRateX10` 离散档位 `{200, 150, 100}`；发送节拍用 tick 取模实现（200 → 每 tick；150 → 每 3 tick 发 2 次；100 → 每 2 tick 发 1 次），`sendSnapshot` 与事件帧解耦（事件帧仍每 tick 发送）。
- [x] 6. `room.ts`：降档条件（任一满足即降一档，最小 100）：①上一秒最慢会话 `bufferedAmount > LIMITS.maxBufferedBytes / 2`；②本秒 `metrics.tickSkips` 增长；③本秒 `ac_room_budget_exceeded_total` 增长。升档条件：连续 3 秒无上述条件则升一档（最大 200）。计数 `snapshotRateDownshifts`。
- [x] 7. `metrics.ts`：新增 `snapshotRateX10`（gauge，取各房间最小值）与 `snapshotRateDownshifts`（counter），`renderPrometheus` 输出 `ac_snapshot_rate_x10`、`ac_snapshot_rate_downshifts_total`。
- [x] 8. `client/net/state.ts`：`INTERPOLATION_DELAY_MS` 改为动态值——快照到达间隔的**中位数**（滑动窗口 60 个样本）× 2，钳制在 `[100, 250]` ms；窗口未填满时用 `snapshotRateX10` 推算的间隔 × 2 兜底。`connection.ts` 在 welcome/pong 后调用 `view.setSnapshotRateX10(rate)`。
- [x] 9. 测试：
  - `snapshot-codec.test.ts`：①无增删的稳态帧仍每 40 tick 全量、其余为差分；②删除列表升序且只含实际删除项；③`presentIds` 在 spawn/despawn 交替 200 次后仍升序且无重复；④与旧实现的字节序列逐字节一致（保留既有断言不修改）。
  - `snapshot.test.ts`：截断场景下选出的实体集合与按距离排序取前 N 的旧行为一致（允许同距离时的 id 决胜）。
  - `state.test.ts`：间隔 33ms → 延迟钳到 100ms；间隔 66ms → 132ms；间隔 200ms → 钳到 250ms。
  - `replication.test.ts`：注入高 `bufferedAmount`/`tickSkips` 后速率降档且帧数符合离散节拍；3 秒后升档。
- [x] 10. 复跑并落证据：`node tools/bots.mjs --players 4 --minutes 5 --strict --out docs/evidence/bots-o05.json`（默认档位，期望 S5.2-1/2/3 全 PASS）；`node tools/bench-sim.mjs --sheep 60 --ticks 6000` 记录编码段耗时改善。
- [x] 11. 回填 `docs/验收报告.md` §3.2：写明"速率默认仍 20Hz；降档是拥塞响应能力，压测默认档位下 S5.2-3 仍 PASS"，并给出新指标取数命令。

**执行记录（2026-09-22，本机 Node v24.14.1）**：11 条任务全部落地（`pnpm check` 退出码 0；shared 149 / server 113 用例全绿）。与计划的差异（均为**加性**或**实现细节**，已同步 §5 调整记录）：

| 任务 | 差异 |
|---|---|
| 1 | `SnapshotBaseline` 除 `presentIds`/`presentCount` 外，另加内部字段 `nextPresentIds`/`nextPresentCount`（本帧构建下一份列表，帧末整体换入）——冻结接口只列了前两项 |
| 1/2 | 升序维护用共享的 `addIdAscending`/`removeIdAscending`（二分定位 + `copyWithin`），插入与删除都不分配 |
| 2 | 解码时只在 `present[id]` 由 0 变 1 时插入、由 1 变 0 时移除；`baselineTick === 0` 只清 `idCount`（stale 尾巴由计数无效化） |
| 4 | `playersCentroid` 改名 `updatePlayersCentroid`（返回 void，写模块级 `centroidScratch`）；堆用三件并行数组（`Entity[]` + `Float64Array` 距离 + `Int32Array` id），容量取 `NET.snapshotMaxEntities` |
| 5 | 节拍判定抽成导出函数 `shouldSendSnapshot(rateX10, tick)`（供单测白盒断言）；事件帧路径未改动，仍每 tick 广播 |
| 6 | 触发口径实现为「距上次评估 ≥1000ms」+「相对上次评估的 `tickSkips`/`roomBudgetExceeded` 是否增长」+「最慢会话 `bufferedAmount > maxBufferedBytes/2`」；`rateCheckAtMs = nowMs`（不累加，评估点会有轻微漂移） |
| 7 | 另加 `http.ts` 的 `minSnapshotRateX10(game)`（取各房间最小值）与 `metrics.test.ts` 的 gauges 字面量补字段 |
| 8 | 视图新增 `setSnapshotRateX10`/`getInterpolationDelayMs`；窗口未满 60 样本时用 `10000/rateX10`（= 默认 200 时 50ms）推算；`connection.ts` 只在 pong 里回填（welcome 不带该字段，协议未改） |
| 9 | 降档用例注入的是 `metrics.tickSkips` 增长（不是 `bufferedAmount`，内存适配器没有可写的积压）；断言 100 档下 40 tick 恰发 20 条快照 |
| 10 | `bench-sim.mjs` 新增编码段（4 会话各持一份 baseline，按房间的 `periodicFull` 策略逐 tick 编码），输出 `encodeSnapshot p50/p95/p99`；`--label` 写 JSON |

## 5. 冻结契约

```ts
// packages/shared/src/net/codec.ts
export interface SnapshotBaseline {
  // …既有 present/nextPresent/bytes/tick…
  readonly presentIds: Uint16Array;   // 上一 tick 已发布实体 id，升序紧凑
  presentCount: number;
}
export interface SnapshotMirror {
  // …既有字段…
  readonly ids: Uint16Array;   // 当前存在实体 id，升序紧凑（原为 number[]）
  idCount: number;
}

// packages/shared/src/net/protocol.ts → LIMITS 新增
snapshotRateLevels: readonly number[];   // [200, 150, 100]，单位 1/10 Hz
snapshotRateDownshiftMs: 3000;           // 无拥塞持续多久升一档
```

| 冻结项 | 值 / 定义 |
|---|---|
| 线上字节格式 | **完全不变**（删除列表升序、记录 15 字节、帧头不变）；本步不得改协议 |
| 速率档位 | 200 = 每 tick；150 = 每 3 tick 发 2 次；100 = 每 2 tick 发 1 次（离散，白盒可断言） |
| 降档触发 | 最慢会话 `bufferedAmount > maxBufferedBytes / 2` 或 `tickSkips` 增长或 `ac_room_budget_exceeded_total` 增长 |
| 升档触发 | 连续 `3000ms` 无触发条件，升一档，上限 200 |
| 插值延迟 | `clamp(2 × medianSnapshotIntervalMs, 100, 250)`；样本不足 60 时用 `snapshotRateX10` 推算 |
| 全量帧节拍 | 保持 `LIMITS.fullSnapshotIntervalTicks = 40`（降档时仍按 tick 计数判定） |
| 事件帧 | 不受速率档位影响，仍每 tick 广播（`room.ts` 的事件帧路径与 `sendSnapshot` 解耦） |

### 5.1 执行期差异（均为加性/内部实现，未改上表任何语义）

| # | 差异 | 理由 |
|---|---|---|
| 1 | `SnapshotBaseline` 增内部字段 `nextPresentIds` / `nextPresentCount` | 本帧要先构建「下一份列表」再整体换入，否则删除要现算；帧末 `presentCount = nextPresentCount` 后清零 |
| 2 | 列表维护抽成共享函数 `addIdAscending` / `removeIdAscending`（二分 + `copyWithin`） | 编解码两侧同一套升序语义，插入/删除都零分配 |
| 3 | `playersCentroid` → `updatePlayersCentroid`（写模块级 `centroidScratch`，返回 void） | 与 `snapshotWorld` 同风格：投影过程不新增每帧对象 |
| 4 | 截断用「有界最大堆」三件并行数组（`Entity[]` + `Float64Array` 距离 + `Int32Array` id），容量 `NET.snapshotMaxEntities` | 平局按 id 升序决胜，等价于旧的两段排序；堆不分配、只保留容量个候选 |
| 5 | `room.ts` 导出 `shouldSendSnapshot(rateX10, tick)` | 节拍是纯函数，单测可白盒断言（150 档的「每 3 tick 发 2 次」相位必须唯一确定） |
| 6 | 触发评估「距上次评估 ≥1000ms」而非按绝对秒对齐；`rateCheckAtMs = nowMs` | 与房间 tick 相位解耦；代价是评估点轻微漂移（不影响 3000ms 升档判定：`rateCleanMs` 按 1000ms 累加） |
| 7 | `http.ts` 增 `minSnapshotRateX10(game)`（无房间时返回默认 200）；`SnapshotViewInternal` 增 `setSnapshotRateX10` / `getInterpolationDelayMs` | 前者是 gauge 的取值口径，后者是渲染路径与单测的读出口 |
| 8 | `tools/bench-sim.mjs` 增编码段（4 会话各持一份 baseline，按房间的 `periodicFull` 策略逐 tick 编码，单独计时、不计入 `total`） | §6 #5 要求编码段绝对值；原工具只测 `stepWorld`/`snapshotWorld` |

## 6. 验证

| # | 命令 | 期望 | 失败意味着 |
|---|---|---|---|
| 1 | `pnpm --filter @ac/shared test` | 全绿，`snapshot-codec.test.ts` 既有字节断言未修改即通过 | 字节格式被改了（会直接打断客户端兼容） |
| 2 | `pnpm --filter @ac/server test` | 全绿（含降档/升档用例） | 档位节拍或触发条件写错 |
| 3 | `node tools/bots.mjs --players 4 --minutes 5 --strict --out docs/evidence/bots-o05.json` | S5.2-1/2/3 与其余门槛 PASS | 默认档位被意外改动，或紧凑列表导致漏发/多发记录 |
| 4 | `curl -s localhost:8787/metrics | grep -E "snapshot_rate"` | 两条指标存在，默认 `ac_snapshot_rate_x10 200` | 指标未接线 |
| 5 | `node tools/bench-sim.mjs --sheep 60 --ticks 6000` | 编码段耗时相对 O04 基线不退化（并记录绝对值） | 部分选择实现比排序更慢（堆实现有误） |
| 6 | `pnpm check` | 退出码 0 | 任一门失败 |
| 7 | 人工复核：`grep -n "id <= MAX_ENTITIES" packages/shared/src/net/codec.ts` | 无命中（或仅剩基线全清那一处） | 1024 全扫未清干净 |

**实测（2026-09-22，本机 Node v24.14.1，逐条执行）**

| # | 结果 | 关键数字 |
|---|---|---|
| 1 | PASS | `pnpm --filter @ac/shared test`：20 文件 / **149 用例**全绿；`snapshot-codec.test.ts` 的既有字节断言**一行未改**即通过（首帧逐字节等于服务器投影、逐帧差分收敛、强制全量清残留）；新增 3 条不变量用例（无增删稳态帧 = 0 记录 0 删除且 `size = 16 + 1`、删除列表升序且只含实际删除 id、200 次 spawn/despawn 后 `presentIds` 与 `mirror.ids` 双侧严格升序且无重复） |
| 2 | PASS | `pnpm --filter @ac/server test`：**113 用例**全绿，含 `replication.test.ts` 新增「离散节拍 200/150/100」与「`tickSkips` 增长 → 150 → 100（最低档不重复计数）→ 100 档下 40 tick 恰发 20 条 → 无拥塞每 3 秒升一档 → 回到 200」 |
| 3 | PASS（`S5.2-9b` 未测） | `docs/evidence/bots-o05.json`：`S5.2-1` 快照均值 **121.7B** ≤ 1200、`S5.2-2` 每客户端带宽 **2.47 KB/s** ≤ 40、`S5.2-3` 速率 **20.02/s** ∈ [19,21]（默认档位未被改动）；`S5.2-8` 硬纠正 **0**；`S5.2-4` 走**替代判据** PASS（schedule error p95 **10.2ms**，前后 1/3 差 **0.55ms** ≤ 2ms、`ac_sim_drift_ms` **13ms** ≤ 50ms、tick work p95 **0.919** / p99 **3.049ms**、预算超出 **0**）。`verdict=fail`/退出码 1 仅因 `S5.2-9b` 未测量（与 O02/O03/O04 同口径） |
| 4 | PASS | `curl -s localhost:8787/metrics \| grep -E "snapshot_rate"`：`ac_snapshot_rate_x10 200`（HELP/TYPE + gauge）、`ac_snapshot_rate_downshifts_total 0`（counter） |
| 5 | PASS | `tools/bench-sim.mjs --sheep 60 --ticks 6000`：编码段（4 会话）p50 **0.030** / p95 **0.046** / p99 **0.086ms**；同脚本同参的 `baseline-o04` 对照 stepWorld p95 **0.092 vs 0.088ms**、snapshotWorld p95 **0.003 vs 0.003ms**、单 tick 合计 p95 **0.094 vs 0.091ms**（同量级，±5% 噪声内）、稳态 raw **312.6 vs 336.9 B/tick**，`verdict=pass`（证据累加在 `docs/evidence/bench-sim-60sheep.json` 的 `runs.after-o05` / `runs.baseline-o04`） |
| 6 | PASS | `pnpm check` 退出码 **0**（typecheck + eslint/prettier + 3 project vitest **418 用例** + check:docs + check:assets + check:count + check:coverage） |
| 7 | PASS | `grep -n "id <= MAX_ENTITIES" packages/shared/src/net/codec.ts`：**0 命中**（解码不再重建整表；1024 全扫只剩 `SnapshotMirror.bytes` 的逐字节清零，与 id 无关） |

## 7. DoD（验收标准）

- [x] `pnpm check` 全绿；`snapshot-codec.test.ts` 的既有字节断言未做任何修改。
- [x] `grep -c "id <= MAX_ENTITIES" packages/shared/src/net/codec.ts` ≤ 1（只允许基线全清那一处保留）。
- [x] `mirror.ids` 为 `Uint16Array` 且解码路径不再重建整表（代码审查可判定）。
- [x] 截断路径只有一次针对结果集的排序（`snapshot.test.ts` 断言选择集合与旧行为一致）。
- [x] 自适应速率：`replication.test.ts` 覆盖降档/升档；`ac_snapshot_rate_x10` 与 `ac_snapshot_rate_downshifts_total` 在 `/metrics` 可见。
- [x] 客户端插值延迟随快照间隔变化且钳制在 `[100, 250]` ms（`state.test.ts` 覆盖）。
- [x] `docs/evidence/bots-o05.json` 存在且 S5.2-1/2/3 PASS；`docs/验收报告.md` §3.2 已回填。
- [x] 未新增运行时依赖；未改 `SERVER_TICK_MS`、`SNAPSHOT_RECORD_BYTES`、`fullSnapshotIntervalTicks`。

## 8. 风险与回滚

| 风险 | 触发信号 | 对策 |
|---|---|---|
| 紧凑列表顺序不稳导致快照字节不可复现 | `snapshot.test.ts` 逐字节断言失败 | 删除列表强制升序输出；`presentIds` 维护时保持升序；测试断言"升序且无重复" |
| `mirror.ids` 类型从 `number[]` 改为 `Uint16Array` 破坏客户端消费点 | `state.ts` 的类型检查或运行时报错 | `idCount` 取代 `.length`；先在 `shared` 内部完成替换并跑 `pnpm typecheck`，再改客户端 |
| 降档让玩家感到卡顿 | 压测中 `snapshotArrivalJitterP95Ms` 上升或掉帧 | 默认不下调；只有在实测拥塞指标触发时才降，且最低 10Hz；降档事件计数可在 O10 加告警 |
| 插值延迟过大导致手感"糊" | 实测 p95 延迟 > 250ms 或位置滞后明显 | 上限硬钳 250ms；间隔中位数用 60 样本窗口，避免抖动导致频繁改变 |
| 部分选择算法在"全部实体等距"时与旧行为不同 | `snapshot.test.ts` 失败 | 平局统一按 id 升序决胜（与旧实现的两段排序等价），写进 §5 |
| 回滚目标 | — | 回滚 = `git revert` 本次提交（codec/snapshot/room/metrics/client state+connection + 测试 + 证据），回到 HANDOFF-O04 状态；协议未变，无需客户端兼容处理 |

## 9. 移交物

**移交物 ID**：HANDOFF-O05

给下一步（O06 客户端每帧分配与 UI 节流）的稳定接口与已验证能力：

**稳定接口**
- `SnapshotBaseline.presentIds/presentCount`、`SnapshotMirror.ids/idCount`（升序紧凑，`Uint16Array`）
- `LIMITS.snapshotRateLevels = [200, 150, 100]`、`LIMITS.snapshotRateDownshiftMs = 3000`
- 指标 `ac_snapshot_rate_x10`、`ac_snapshot_rate_downshifts_total`
- `SnapshotView.setSnapshotRateX10(rate)`（客户端插值延迟的输入之一）

**已验证能力**
- 编码/解码不再有 1024 槽全扫；删除列表仍升序、线上字节与旧实现逐字节一致
- 快照率可自适应（默认 20Hz 不变），并且事件帧不受影响
- 客户端插值延迟随实测快照间隔自适应，钳制在 100–250ms
- 默认档位下 bots 的 S5.2-1/2/3 仍 PASS（证据留档）

**未决项（移交后续）**
- 降档是否该上报给客户端 UI（当前只在指标里可见）→ 若需要，归入 O06 的调试面板项
- `ac_snapshot_rate_downshifts_total` 的告警阈值 → 记入 O10 的门槛登记
