# O04 模拟热路径事件池与空间划分

> 里程碑：O4 ｜ 预估：1.5 会话（a/b 两块，见 §4） ｜ 上游移交物：HANDOFF-O03

## 0. 现状与不足（证据）

本步处理**模拟内核的每 tick 常数**：分离与邻居聚集都是 O(n²)，事件每 tick 新建，计数/排序/净化各自重复了一遍。
这些成本在 4 人 60 羊的满编局里直接决定单 tick 工作量——也就是 O02 引入的 `LIMITS.roomTickBudgetMs = 8` 能不能守住。

### 0.1 结论一：实体分离是 O(passes × n²)，且每对都做两次查表

| 事实 | 证据 |
|---|---|
| 双层循环 + 每对 `getEntity` 两次 + `radiusOf` 两次 + `Math.sqrt` | `packages/shared/src/sim.ts:210-249` |
| 每个 pass 之后又全量跑一遍 `collideStatic` | `packages/shared/src/sim.ts:244-249` |
| `separationPasses = 1`（上调即线性放大成本） | `packages/shared/src/config/entity.ts:21-22` |
| `getEntity` 每次带边界检查 | `packages/shared/src/world.ts:167-174` |
| 规模 | 4 玩家 + 60 羊 = 64 实体 → 2016 对/次，20Hz 下约 4 万次配对判定/秒 |

### 0.2 结论二：邻居聚集每羊全扫全部实体，且"最近 12 个"实际是"最先遇到的 12 个"

| 事实 | 证据 |
|---|---|
| `gatherNeighbors` 遍历 `world.activeIds` 全部实体，只按距离半径过滤 | `packages/shared/src/ai/flocking.ts:75-92` |
| 容量满即丢弃（`addNeighbor` 里 `if (neighbors.count >= neighbors.capacity) return`） | `packages/shared/src/ai/flocking.ts:47-60`（容量检查在 :54）、容量 12（`packages/shared/src/config/sheep.ts` 的 `maxNeighbors: 12`） |
| 规模 | 60 羊时每个羊 63 次距离计算 → 约 3780 次/tick |
| 行为影响 | 丢弃与距离无关，只与 `activeIds` 顺序（id 升序）有关 → 羊多时最近的邻居可能被丢掉，flock 转向依赖 id，属**正确性**问题而非仅性能问题 |

### 0.3 结论三：每 tick 事件对象全部分配

| 事实 | 证据 |
|---|---|
| `pushEvent` 内 `createSimEvent()` 新建对象 | `packages/shared/src/combat/resolve.ts:184-205`（`createSimEvent()` 调用在 :195）、`packages/shared/src/world.ts:103-105` |
| 另一处直接字面量 push | `packages/shared/src/ai/sheepAttack.ts:30-42` |
| 每 tick 开头 `world.events.length = 0` 丢弃引用 | `packages/shared/src/sim.ts:44` |
| 典型事件量 | 霰弹枪 8 pellet（`packages/shared/src/config/weapons.ts:53`）每次命中一条 `playerHit`；`reviveProgress` 按 5% 步进最多 20 条/人；每次击杀一条 `sheepKilled` |

### 0.4 结论四：射线路径有每调用数组分配（量级小，属清理项）

| 事实 | 证据 |
|---|---|
| `rayVsAabb` 每次调用新建 4 个数组 | `packages/shared/src/combat/raycast.ts:136-139` |
| `traceRay` 对全部活动实体做胶囊求交，无廉价早退 | `packages/shared/src/combat/resolve.ts:89-160` |
| 量级（诚实标注） | 每次 `traceRay` 4 个数组；霰弹枪一次开火 8 pellet → 32 个数组/次开火，射速下约 40 个数组/秒——**不是瓶颈**，本步一起清掉是为了让 §4 的零分配断言在整条热路径上成立 |

### 0.5 结论五：同一遍历被写了三遍，每 tick 还排一次序

| 事实 | 证据 |
|---|---|
| `aliveSheepCount` 每 tick 全扫（由 `updateDirector` 调用） | `packages/shared/src/ai/director.ts:96-104,196` |
| 与 `countActive` 重复实现 | `packages/shared/src/world.ts:232-242` |
| `collectDirectorPlayerIds` 每 tick `push` + `sort`，而 `activeIds` 本就按 id 升序 | `packages/server/src/room.ts:409-419`、`packages/shared/src/world.ts:172-178` |
| `buildCommands` 对每个玩家在 `room.sessions` 里线性查找 | `packages/server/src/room.ts:317-345` |

### 0.6 结论六：命令被净化两次

| 事实 | 证据 |
|---|---|
| 会话边界已净化（服务器入口） | `packages/server/src/session.ts:300` |
| 模拟内核内又净化一次（每 tick 每玩家） | `packages/shared/src/sim/localStep.ts:34` |
| 客户端预测与 bots 也走同一路径 | `packages/client/src/prediction/predictor.ts:61-68`、`tools/bots.mjs` 直接 import `stepLocalPlayer` |

### 0.7 结论七：没有 60 羊的性能基准，因此"≤30% 单核"从未测量

| 事实 | 证据 |
|---|---|
| 分配探针存在但无 npm 入口，且场景实体数很小 | `tools/check-alloc.mjs:11-15`、`package.json:14-24`（无 `check:alloc` 脚本） |
| soak 的 60 羊 CPU 门槛要求羊峰值 ≥40 才取样 | `tools/soak.mjs:287-295` |
| 实测羊峰值只有 22，门槛记 `not-measured` | `docs/验收报告.md` §3.3 |

## 1. 目标

本步骤结束后，模拟内核的每 tick 常数与实体数近似线性：

1. 实体分离与邻居聚集走**同一个空间网格**（cell 边长 = `SHEEP_AI.neighborRadiusM = 3m`），配对判定只发生在同格与相邻格；
2. 事件对象**零分配**（池化复用），并把"事件对象仅当 tick 内有效"写成冻结契约；
3. 邻居"最近 12 个"按距离选取，与遍历顺序无关；
4. 活动羊数只在 `stepWorld` 里数一次，director 与 metrics 共用该结果；
5. 命令净化只发生在会话边界，模拟内核信任 `Command`；
6. 建立可复现基准 `tools/bench-sim.mjs`（4 玩家 + 60 羊），SLO 与 O02 的房间预算对齐：
   **单 tick（`stepWorld` + `snapshotWorld`）p95 ≤ 8ms、p99 ≤ 12ms**，且不得比本步开工前测得的基线更慢。

## 2. 入口条件

**入口条件**：HANDOFF-O03

| # | 必须已成立的事实 | 验证命令 | 期望 |
|---|---|---|---|
| 1 | 上一步交付可用 | `pnpm check` | 退出码 0 |
| 2 | 出站帧所有权已落地（保证后续放大流量不会损帧） | `grep -n "bufferedAmount" packages/server/src/transport/types.ts` | 命中 |
| 3 | 房间预算已存在（本步 SLO 的由来） | `grep -n "roomTickBudgetMs" packages/shared/src/net/protocol.ts` | 命中 `8` |
| 4 | 基线与门槛可读 | `node --expose-gc tools/check-alloc.mjs` | 退出码 0，打印 raw/retained 每 tick 字节 |
| 5 | 确定性基线可跑 | `pnpm --filter @ac/shared test` | 全绿（含 `determinism.test.ts`） |

## 3. 交付物

| 文件 | 状态 | 职责 |
|---|---|---|
| `packages/shared/src/sim/spatialGrid.ts` | 新建 | 零分配均匀网格：计数排序建表 + 3×3 邻格遍历 |
| `packages/shared/src/sim.ts` | 修改 | 分离走网格；事件池；`world.stats.aliveSheep` 单次统计 |
| `packages/shared/src/ai/flocking.ts` | 修改 | 邻居走网格 + 按距离有界插入（顺序无关） |
| `packages/shared/src/ai/director.ts` | 修改 | `aliveSheepCount` 读 `world.stats` |
| `packages/shared/src/combat/raycast.ts` | 修改 | `rayVsAabb` 去数组分配 |
| `packages/shared/src/combat/resolve.ts` | 修改 | `pushEvent` 走事件池；`traceRay` 廉价早退 |
| `packages/shared/src/ai/sheepAttack.ts` | 修改 | 事件改走统一 `pushEvent`（去字面量） |
| `packages/shared/src/world.ts` | 修改 | `World.stats`；`events` 池的创建/复用原语 |
| `packages/shared/src/sim/localStep.ts` | 修改 | `applyCommandToState` 不再净化（职责移交会话边界） |
| `packages/server/src/room.ts` | 修改 | `sessionsByPid` 索引；`collectDirectorPlayerIds` 去排序 |
| `packages/shared/src/sim/spatialGrid.test.ts` | 新建 | 网格建表/邻格覆盖/边界用例 |
| `packages/shared/src/sim/steadyState.test.ts` | 新建 | 稳态零分配：固定实体集连续 600 tick，事件对象引用集合不变 |
| `packages/server/src/session.test.ts` | 修改 | 净化职责仍生效（越界命令在会话边界被夹取） |
| `tools/bench-sim.mjs` | 新建 | 进程内 4 玩家 + 60 羊基准，报 p50/p95/p99 单 tick 耗时 |
| `package.json` | 修改 | 新增 `check:alloc`、`bench:sim` 脚本 |
| `docs/evidence/bench-sim-60sheep.json` | 新建 | 修复前后对比证据 |
| `docs/验收报告.md` | 修改 | 回填 §3.3 的 60 羊 CPU 口径与新增基准结论 |

## 4. 任务清单

### O04a — 网格、射线、计数（第 1 块）

- [ ] 1. `spatialGrid.ts`：`createSpatialGrid(cellSizeM, capacity)` 返回可复用结构（`Int32Array` 的 `cellStart`/`cellItems`、`cellCount`）；`buildSpatialGrid(grid, world)` 用两趟计数排序把活动实体按格写入（只统计 `player` / `sheep`，跳过 `projectile`）；`forEachNeighbor(grid, x, z, radiusM, visit)` 遍历覆盖半径的整数格。
- [ ] 2. `sim.ts`：`resolveEntitySeparation` 改为网格驱动——对每个实体只检查同格与右/下四邻格（避免重复对），重叠判定仍用 `radiusOf(a)+radiusOf(b)`；每个 pass 后的 `collideStatic` 保持不变（行为一致性优先）。
- [ ] 3. `flocking.ts`：`gatherNeighbors` 改为网格查询 + `neighborRadiusM` 过滤；把 `addNeighbor` 改成"有界按距离插入"（容量 12，最远的被挤掉），使结果与遍历顺序无关。
- [ ] 4. `director.ts` + `sim.ts` + `world.ts`：`stepWorld` 末尾统计一次活动羊数写入 `world.stats.aliveSheep`；`aliveSheepCount(world)` 改为返回该值（保留函数名，内部不再遍历）；`countActive` 保持原语义不动。
- [ ] 5. `raycast.ts`：`rayVsAabb` 去掉 4 个数组，改用标量比较（或用模块级 `Float64Array(3)` 复用）。
- [ ] 6. `resolve.ts`：`traceRay` 增加廉价早退（实体到射线的平方距离 > 有效半径平方或 > `bestT` 对应距离则跳过），保持命中结果与旧实现**逐字节一致**（用既有测试 + 新增对拍断言）。
- [ ] 7. `room.ts`：新增 `sessionsByPid: Map<number, Session>`（join/leave/reconnect 时维护），`buildCommands` 用它取命令；`collectDirectorPlayerIds` 去掉 `sort`（`activeIds` 已升序），并加注释说明依赖。

### O04b — 事件池、净化契约、基准与证据（第 2 块）

- [ ] 8. `world.ts`：`World` 增加 `readonly eventPool: SimEvent[]`（容量 `LIMITS.eventPoolSize = 256`）与 `eventCursor`；`events` 数组语义不变（每 tick 清引用），`pushEvent` 从池取对象填充。
- [ ] 9. `resolve.ts` 与 `sheepAttack.ts`：两处 `pushEvent` 统一为 `@ac/shared` 的单一实现（`sim/events.ts` 新建），删除 `createSimEvent()` 的逐事件调用。
- [ ] 10. `localStep.ts`：`applyCommandToState` 去掉 `sanitizeCommand` 调用（改为直接读字段），把"输入净化是会话边界职责"写进函数注释；`stepLocalPlayer` 同步调整。
- [ ] 11. 测试：`spatialGrid.test.ts`（同格/跨格/边界/空世界）；`steadyState.test.ts`（固定 64 实体跑 600 tick，断言事件对象引用集合大小恒定、`grid` 结构引用不变）；`resolve.test.ts` 增补"网格化前后射线结果一致"的对拍断言（构造 20 组固定姿态，逐条比较 `ShotTrace` 五个字段）。
- [ ] 12. `tools/bench-sim.mjs`：进程内构造 4 玩家 + 60 羊（固定种子），预热 500 tick 后测 6000 tick，输出 `p50/p95/p99/max` 的单 tick 耗时（分 `stepWorld` 与 `snapshotWorld` 两段）到 stdout 与 ``json` 文件。
- [ ] 13. `package.json`：新增 `"check:alloc": "node --expose-gc tools/check-alloc.mjs"`、`"bench:sim": "node tools/bench-sim.mjs --sheep 60 --ticks 6000"`（`check:alloc` 接入 `pnpm check` 由 O10 负责）。
- [ ] 14. 证据：先在本步开工前跑一次 `node --expose-gc tools/check-alloc.mjs` 与 `node tools/bench-sim.mjs --sheep 60` 记基线，改动后再跑一次，把两组数字写入 `docs/evidence/bench-sim-60sheep.json`（含 `p95`、`p99`、`rawPerTick`、`retainedPerTick`）。
- [ ] 15. 回填 `docs/验收报告.md` §3.3：把"60 羊 CPU 门槛 not-measured"改为"以 `tools/bench-sim.mjs` 的单 tick p95 作为代理指标，实测 X ms ≤ 8ms"，并说明与 soak 的 CPU 口径关系。

## 5. 冻结契约

```ts
// packages/shared/src/sim/spatialGrid.ts
export interface SpatialGrid {
  readonly cellSizeM: number;
  readonly cols: number;
  readonly rows: number;
  readonly cellStart: Int32Array;   // 长度 cols*rows + 1
  readonly cellItems: Int32Array;   // 长度 capacity
  readonly itemCount: number;
}
export function createSpatialGrid(cellSizeM: number, capacity: number): SpatialGrid;
export function buildSpatialGrid(grid: SpatialGrid, world: World): SpatialGrid;
export function forEachNeighbor(
  grid: SpatialGrid,
  x: number,
  z: number,
  radiusM: number,
  visit: (entityIndex: number) => void,
): void;

// packages/shared/src/world.ts
export interface WorldStats {
  aliveSheep: number;   // 每 tick 由 stepWorld 写一次；director 与 metrics 只读
}

// packages/shared/src/net/protocol.ts → LIMITS 新增
eventPoolSize: 256,     // 事件对象池容量；超过容量的事件被丢弃并计 metrics.eventsDropped
```

| 冻结项 | 值 / 定义 |
|---|---|
| 网格 cell 边长 | `SHEEP_AI.neighborRadiusM = 3m`（一格即一个邻居半径） |
| 3×3 覆盖的证明 | 最大实体半径和为 king 1.6 + elite 0.6 = 2.2 < 3，故任意可能重叠（或互为邻居）的两个实体所在格的轴向索引差 ≤1 → 3×3 邻域足够 |
| `maxNeighbors` 语义 | "距离最近的 12 个"（顺序无关）；超出半径的一律不入选 |
| 事件对象生命周期 | 事件对象**仅在当 tick 内有效**：`pushEvent` 会复用池对象，消费方必须在同一 tick 内读完（服务器广播、测试断言都在 tick 内） |
| 净化职责 | 输入净化在**会话边界**（`session.ts` 的 `sanitizeCommandFields`）与客户端采样（`sampler`）；模拟内核信任 `Command`，不再夹取 |
| 射线一致性 | 网格/早退优化不得改变 `ShotTrace` 的 `hit/targetId/part/distanceM/x/y/z`（对拍断言保证） |
| 基准 SLO | 60 羊 + 4 玩家：单 tick p95 ≤ 8ms、p99 ≤ 12ms；且不得慢于本步开工前基线 |

## 6. 验证

| # | 命令 | 期望 | 失败意味着 |
|---|---|---|---|
| 1 | `pnpm --filter @ac/shared test` | 全绿（含 `determinism.test.ts` 逐字节断言、`resolve.test.ts` 对拍断言） | 网格化改变了模拟结果或破坏了确定性 |
| 2 | `pnpm --filter @ac/server test` | 全绿 | 净化职责移交漏了入口，或 `sessionsByPid` 索引未同步（掉命令） |
| 3 | `node --expose-gc tools/check-alloc.mjs` | 退出码 0，`rawPerTick ≤ 8`、`retainedPerTick ≤ 16` | 事件池或网格仍有稳态分配 |
| 4 | `node tools/bench-sim.mjs --sheep 60 --ticks 6000` | `p95 ≤ 8ms`、`p99 ≤ 12ms` 且不慢于基线 | 网格没生效（退回全量循环）或 `forEachNeighbor` 遍历了过多格子 |
| 5 | `node tools/bots.mjs --players 4 --minutes 5 --strict --out /tmp/bots-after-o04.json` | 全部门槛 PASS | 数值或事件语义被改动（例如事件池导致漏事件） |
| 6 | `pnpm check` | 退出码 0 | 任一门失败 |
| 7 | 人工复核：`grep -n "sanitizeCommand" packages/shared/src/sim/localStep.ts packages/server/src/session.ts` | 只命中 `session.ts` | 内核仍在净化，或会话边界漏了净化 |

## 7. DoD（验收标准）

- [ ] `pnpm check` 全绿；`determinism.test.ts` 的逐字节断言未修改即通过。
- [ ] `tools/bench-sim.mjs` 在 4 玩家 + 60 羊下 `p95 ≤ 8ms`、`p99 ≤ 12ms`，且相对开工前基线不退化（两组数字都在 `docs/evidence/bench-sim-60sheep.json` 里）。
- [ ] `node --expose-gc tools/check-alloc.mjs` 通过（`rawPerTick ≤ 8`、`retainedPerTick ≤ 16`）。
- [ ] 邻居选取与遍历顺序无关（`flocking.test.ts` 增补"打乱 `activeIds` 顺序后 flock 力一致"用例）。
- [ ] `gatherNeighbors` / `resolveEntitySeparation` / `aliveSheepCount` 三处都不再全量遍历实体。
- [ ] `docs/验收报告.md` §3.3 的 60 羊口径已回填；`docs/evidence/bench-sim-60sheep.json` 存在。
- [ ] 未新增运行时依赖；未改 `SERVER_TICK_MS`、协议字段、量化规则；未改 `stepWorld` 的阶段顺序（`packages/shared/src/sim.ts:36-60` 的调用次序）。

## 8. 风险与回滚

| 风险 | 触发信号 | 对策 |
|---|---|---|
| 网格化让模拟结果与旧实现不一致（分离顺序、邻居集合变化） | `determinism.test.ts` 或 `resolve.test.ts` 对拍失败；bots 的命中/击杀与基线差异大 | 分离仍在网格内按 id 升序处理配对，保持"先出现的实体先被推"的既有顺序；对拍断言先在 O04a 全绿再进 O04b |
| 事件池引入"跨 tick 读到被覆写的事件" | 客户端出现重复/错乱事件；`encodeEventFrame` 输出异常 | 事件只在 `runTick` 内被编码（同步），文档把语义写进 §5；测试断言"tick 内读取" |
| 事件池容量不足导致丢事件 | `metrics.eventsDropped > 0` | 容量 256 覆盖 4 人 × 20 条 `reviveProgress` + 命中事件；超限计数并保留日志，容量可在 §5 上调 |
| `activeIds` 升序假设被后续改动破坏 | `collectDirectorPlayerIds` 输出未排序，导演分配错位 | 加断言（`dev` 环境）+ 注释说明依赖；若需鲁棒则保留一个廉价的"已排序"检查 |
| 净化职责移交造成作弊面 | 客户端可发送越界轴/角度 | 净化在**会话边界**（服务器唯一入口）执行，覆盖所有客户端路径；bots 直连 ws 也过同一入口；`session.test.ts` 覆盖 |
| 基准脚本测的是"本机"，数字不可跨机比较 | 换机后 SLO 不达标 | 证据文件必须记录 `os.cpus()[0].model` 与 Node 版本；SLO 只对同一台机器做前后对比 |
| 回滚目标 | — | 回滚 = `git revert` 本次提交（shared/sim + shared/ai + shared/combat + server/room + tools/bench-sim），回到 HANDOFF-O03 状态；无协议与数据变更 |

## 9. 移交物

**移交物 ID**：HANDOFF-O04

给下一步（O05 快照编码增量化与自适应快照率）的稳定接口与已验证能力：

**稳定接口**
- `createSpatialGrid(cellSizeM, capacity)` / `buildSpatialGrid(grid, world)` / `forEachNeighbor(grid, x, z, radiusM, visit)`
- `World.stats.aliveSheep`（每 tick 由 `stepWorld` 写）
- `World.eventPool` + `LIMITS.eventPoolSize = 256`；`pushEvent` 的"仅当 tick 内有效"语义
- `applyCommandToState` 不再净化（契约：净化在会话边界）
- `package.json` 的 `check:alloc` 与 `bench:sim` 脚本；`tools/bench-sim.mjs` 的 `--sheep/--ticks` 参数

**已验证能力**
- 分离与邻居聚集不再 O(n²)；60 羊满编下单 tick p95 ≤ 8ms（基准可复现）
- 事件零分配；`check-alloc` 的 raw/retained 门槛通过
- 活动羊数单次统计，director 与 metrics 共用
- 模拟结果与优化前逐字节一致（确定性测试未改动即通过）

**未决项（移交后续）**
- `metrics.eventsDropped` 的告警阈值（当前只计数）→ 记入 O10 的门槛登记
- soak 的"4 人 60 羊单核 CPU ≤ 30%"仍以基准代理指标代替，真实游戏局内 60 羊的 CPU 采样留待长跑 soak 复测
