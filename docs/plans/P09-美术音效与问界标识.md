# P09 美术、音效与"问界"标识

> 里程碑：M8 ｜ 预估：3 人日 ｜ 上游移交物：HANDOFF-H08

## 1. 目标

本步骤结束后，游戏**看起来与听起来像一个成品**：灰盒变成低多边形风格化牧场；每只羊额头都有程序化绘制的「问界」标识，问界羊与羊王的额标随血量下降而更亮（"越残越亮"）；第一人称有手臂与武器动作；开火有枪口火焰、曳光、弹孔、毛屑；音效全部由 WebAudio 合成（枪声、羊叫、命中、冲锋警报、UI）；HUD 与大厅统一视觉主题，并提供色盲安全与减少动态两项可访问性开关。仓库仍然零外部素材。

## 2. 入口条件

**入口条件**：HANDOFF-H08

| # | 必须已成立的事实 | 验证命令 | 期望 |
|---|---|---|---|
| 1 | 完整对局闭环可玩 | `pnpm dev` 两窗口走 AC-01/AC-02 | 通过 |
| 2 | HUD 根节点已冻结 | `grep -n "#hud" packages/client/index.html` | 存在 `#hud`/`#banner`/`#debug`/`#pause-overlay` |
| 3 | 羊的状态值已冻结 | 阅读 P07 §5.1 | 状态 0–9 一致 |
| 4 | 额标策略已定 | 阅读 [ADR-003](../00-共识/ADR/ADR-003-美术素材与问界标识策略.md) | 原始创作、无真实商标 |
| 5 | 设置项已持久化 | `grep -n "ac.settings.v1" packages/client/src/settings/store.ts` | 存在 |

## 3. 交付物

| 文件 | 状态 | 职责 |
|---|---|---|
| `packages/client/src/render/materials.ts` | 新建 | 共享材质与调色板（含色盲安全变体）、受击白闪、雾气参数 |
| `packages/client/src/render/emblem.ts` | 新建 | **额标**：`CanvasTexture` 程序化绘制（菱形外框 + 问号笔画），生成缓存与自发光强度规则 |
| `packages/client/src/render/sheepModel.ts` | 新建 | 低多边形羊模型拼装（身/头/腿/角）+ 羊毛 `InstancedMesh` 簇，四种羊形共用骨架 |
| `packages/client/src/render/viewmodelModel.ts` | 新建 | 第一人称手臂 + 三把武器模型与动画状态机（待机/走动/开火/换弹/切枪） |
| `packages/client/src/render/particles.ts` | 新建 | 对象池化粒子：枪口火焰、毛屑、弹孔、命中火花、羊死亡碎屑 |
| `packages/client/src/render/effects.ts` | 新建 | 曳光、屏幕暗角与血屏（CSS 层）、命中红闪、狂暴滤镜 |
| `packages/client/src/render/arenaArt.ts` | 新建 | 牧场美术：地面纹理（`CanvasTexture` 草地）、围栏木纹、谷仓、杂物与生成点标记 |
| `packages/client/src/audio/synth.ts` | 新建 | WebAudio 合成原语（噪声 burst、ADSR 包络、滤波器、FM 振荡器） |
| `packages/client/src/audio/sfx.ts` | 新建 | 具体音效配方：枪声 ×3、换弹、命中、爆头、羊叫 ×4、冲锋预警、怒吼（狂暴）、UI 点击、胜负提示 |
| `packages/client/src/audio/mixer.ts` | 新建 | 音量总线（master/sfx/ui）、`PannerNode` 3D 定位、并发上限与距离衰减 |
| `packages/client/src/styles.css` | 修改 | 视觉主题（CSS 变量、字体栈、HUD 排版、可访问性开关的类切换） |
| `packages/client/src/ui/hud.ts` | 修改 | 视觉化：准星扩散、血条护甲、弹药、怒气条、波次条、击杀提示、Boss 血条 |
| `packages/client/src/ui/settings.ts` | 新建 | 设置面板（灵敏度、FOV、音量、色盲安全、减少动态） |
| 上述模块的 `*.test.ts` | 新建 | 额标纹理非空、自发光规则、音效配方参数、粒子池上限 |

## 4. 任务清单

- [x] 写 `emblem.ts`：用 `OffscreenCanvas`（不支持时退回 `document.createElement('canvas')`）绘制 128×128 额标——菱形描边 + 内部问号笔画 + 底衬圆环；导出 `getEmblemTexture()`（单例缓存）与 `emblemIntensity(hpRatio) = 0.2 + 0.8 * (1 - hpRatio)`。
- [x] 把额标贴到羊头部的独立面片（`PlaneGeometry` 贴在头部前侧，`emissiveMap = emblemTexture`，`emissiveIntensity` 由 §5.2 规则驱动，`depthTest` 保持默认）。
- [x] 写 `sheepModel.ts`：共享几何体与材质实例；四种羊形通过缩放与配色区分（`grunt` 白+灰、`ram` 深灰+角、`elite` 米白+发光额标、`king` 加体型 1.6 倍与暗红角）；羊毛用 `InstancedMesh`（每只 12 簇，`grunt` 减到 8 簇）以减少 draw call。
- [x] 写 `viewmodelModel.ts`：手臂用胶囊拼装，武器用长方体拼装（枪管/机匣/弹匣/枪托），独立 `Scene` + 独立相机（避免与主场景的深度冲突），动画用关键帧式插值（位置/旋转/后坐偏移），由状态机驱动（待机呼吸、走动摆动、开火后坐、换弹旋转、切枪下落）。
- [x] 写 `particles.ts`：预分配 256 个粒子槽，全部 `InstancedMesh`；发射器有并发上限与生命周期；不使用外部贴图，用 `CanvasTexture` 生成圆形软点。
- [x] 写 `effects.ts`：曳光用细长 `Mesh` + 快速衰减；暗角/血屏/狂暴滤镜用 CSS 层（`#hud` 内的固定定位 div），避免引入后处理管线（性能预算优先）。
- [x] 写 `arenaArt.ts`：草地/泥土纹理用 `CanvasTexture` 程序化生成（噪声 + 色块），围栏木纹同理；杂物（干草捆、水槽）用基础几何体摆放，且**不参与碰撞**（避免与 P02 的碰撞几何不一致）。
- [x] 写 `audio/synth.ts` + `sfx.ts`：步枪 = 白噪声 burst（10ms 攻击 + 120ms 衰减）经低通 3kHz + 二次谐波；手枪更短更高；霰弹更低更闷；羊叫 = 两个振荡器（基频 220–320Hz）带颤音与带通；冲锋预警 = 上滑正弦 + 噪声；狂暴怒吼 = 低频锯齿 + 失真。所有音效首次交互后才创建 `AudioContext`（浏览器自动播放策略）。
- [x] 写 `mixer.ts`：`masterGain` → `sfxGain`/`uiGain`；3D 音效走 `PannerNode`，距离衰减 `refDistance 3 / maxDistance 40`；同时发声上限 24，超出丢弃最远。
- [x] 写 `settings.ts` + 主题：色盲安全开关切换到色盲友好调色板（避免红绿对立，改用蓝橙对比）；减少动态开关关闭屏幕抖动、暗角脉动与粒子高频抖动；两项都写回 `ac.settings.v1`。
- [x] HUD 视觉化：准星按当前散布实时扩散；血条受伤残影；怒气条满值脉动；波次条显示 10 段进度；击杀提示右上滚动带爆头标记；Boss 血条居顶。
- [ ] 性能：合并静态几何（围栏/地面/杂物各一 draw call）、开启 `frustumCulled`、羊模型复用几何与材质、粒子池上限 256；把 draw call 数与三角面数显示在调试面板。
- [x] 测试：`getEmblemTexture()` 返回非空 `CanvasTexture`；`emblemIntensity(1) == 0.2` 且 `emblemIntensity(0) == 1.0` 且单调递增；音效配方在无 `AudioContext` 环境下不抛异常（惰性初始化）；粒子池在 1000 次发射后长度不超过上限。

> 执行期说明：调试面板的 draw call / 三角面 / 帧时间 P95 读数已实现（`render/renderer.ts` 采集 `webgl.info.render`，`ui/debugPanel.ts` 渲染，`?debug=1` 开启），`test/budget.test.ts` 也在 40 实体静态场景上约束了批次 / 三角形 / 材质上界；但 draw call 与帧时间 P95 的**实测数值必须在真实浏览器中读取**，因此第 12 项保持未勾选（见 §6.1 / §7.1）。

## 5. 冻结契约

### 5.1 额标规范（冻结，回应 FR-09 与 NFR-10）

| 项 | 值 |
|---|---|
| 载体 | 128×128 `CanvasTexture`，透明背景 |
| 构成 | 菱形外框（描边 6px）+ 居中问号笔画 + 底衬圆环，全部由代码绘制 |
| 贴放 | 羊头部前侧独立面片，尺寸 0.22m × 0.22m（羊王 0.36m × 0.36m） |
| 颜色 | 主色 `#e8e8f0`；自发光色 `#7ad1ff`（羊王改 `#ff8a4c`） |
| 自发光强度 | `emblemIntensity(hpRatio) = 0.2 + 0.8 * (1 - hpRatio)` |
| 平滑 | 强度变化带 0.3 秒时间常数，避免血量骤变时闪烁 |
| 合规 | 不含任何真实商标图形、字体或品牌名称；不使用外部图片 |

### 5.2 HUD 与状态呈现规则（冻结）

| UI 元素 | 数据来源 | 呈现规则 |
|---|---|---|
| 准星扩散 | `WeaponState.spreadDeg`（P06） | 映射 0.5°–5° → 4px–24px |
| 血条/护甲 | 本地玩家 `hpRatio` + 护甲 | 受伤后 0.8 秒残影（第二层红条缓慢追上） |
| 弹药 | `WeaponState.magInSlot[activeSlot]` + `reserveAmmo` | 低于 30% 变黄，空弹匣变红并脉动 |
| 换弹进度 | `reloadEndsAtMs` | 准星下方环形进度 |
| 怒气条 | `rage` | 未满：蓝白；满值：橙色脉动 + 提示键位；狂暴中：倒计时边缘高亮 |
| 波次条 | `MatchState.wave` | 10 段，Boss 波段落标记 |
| 击杀提示 | 事件 2 `sheepKilled` | 右上滚动，爆头加图标，3 秒淡出 |
| 伤害数字 | 事件 1 `playerHit` | 命中点漂浮，爆头更大更亮；**不本地自算** |
| 倒地/救援 | 事件 5/6/7 | 全屏灰化 + 底部救援进度环（仅救援者可见进度） |
| 冲锋预警 | 羊 `state == windup` | 地面红环扩散 + 音效（由 `fx` 流触发，不影响模拟） |

### 5.3 性能预算（冻结，回应 NFR-01）

| 项 | 上限 |
|---|---|
| draw call | ≤ 120（稳态、40 实体） |
| 三角面 | ≤ 180k |
| 粒子并发 | ≤ 256 |
| 同时发声 | ≤ 24 |
| 材质实例 | ≤ 24（共享为主） |
| 帧时间 P95 | ≤ 20ms（1080p、像素比 ≤1.5） |

### 5.4 音频与可访问性（冻结）

| 项 | 值 |
|---|---|
| `AudioContext` 创建时机 | 首次用户交互（点击"进入"按钮）之后 |
| 音量总线 | `masterGain` → `sfxGain`（0.8）/ `uiGain`（0.5） |
| 3D 定位 | `PannerNode`，`refDistance 3`、`maxDistance 40`、`rolloffFactor 1.4` |
| 色盲安全 | 蓝橙对比调色板；不依赖红绿区分敌我与危险度（羊的危险度同时用轮廓粗细与额标亮度表达） |
| 减少动态 | 关闭屏幕抖动、暗角脉动、粒子高频抖动；保留所有影响判定的视觉信息 |
| 字幕 | 关键音效（冲锋预警、波次开始、倒地、胜负）在屏幕上给出文字提示 |

## 5.5 实现澄清与披露（P09 执行期）

1. 零外部素材策略保持：所有贴图由 canvas 程序化绘制（`emblem.ts` / `arenaArt.ts` / `particles.ts`），所有音效由 WebAudio 合成（`audio/synth.ts` + `sfx.ts`），未新增任何 npm 依赖（`git diff M7 -- package.json pnpm-lock.yaml packages/client/package.json` 为空）。
2. 额标自发光管线 = `CanvasTexture`（`getEmblemTexture()` 单例）+ 逐实例自发光属性（`instanceEmissiveTint` / `instanceEmissiveIntensity`，`InstancedBufferAttribute`）+ `materials.emblem.onBeforeCompile` 注入 `applyEmblemShaderPatch`。补丁在片元做 `totalEmissiveRadiance *= vEmissiveTint * vEmissiveIntensity`，因此材质的 `emissiveIntensity` 必须 > 0，否则零乘后额标全黑；评审恰好抓到这一点，现已修复为 `emissiveIntensity: 1` 并加回归测试（`materials.test.ts` 断言 > 0）。
3. `render/**` 不得 import `@ac/shared`（eslint `no-restricted-imports` 强制），因此 `render/entityViews.ts` 的 `SNAPSHOT_FLAG_DOWNED` 是共享标志位的本地镜像；`test/entityViewsFlags.test.ts` 断言镜像值等于 `SNAPSHOT_FLAG.downed` 且不等于 `rageMode`（防漂移成 2）。
4. 客户端测试命令是 `pnpm exec vitest run --project client`：`@ac/client` 没有 `test` 脚本（只有 dev/build/preview），所以 `pnpm --filter @ac/client test` 是静默 no-op，不产生任何用例结果。
5. 模块级缓存的 `CanvasTexture` 从不释放：`getEmblemTexture()` 与 `getSoftDotTexture()` 各自单例缓存；`materials.dispose()` 只 `material.dispose()`，`particles.dispose()` 只释放 geometry/mesh，都不释放这两张纹理（`resetEmblemTexture()` 仅测试调用）。
6. 以下评审发现为**有意暂不修复**，本次执行明确披露（不计入本步骤 DoD）：

   1. 每帧仍有分配：`render/entityViews.ts` 的 `SheepInstance` / `ChargeWarning` 对象字面量、`render/effects.ts` 每帧 `new Set()`、`audio/mixer.ts` 的 `{ ...DEFAULT_LISTENER }` / `{ ...pose }` 展开、`main.ts` 帧体内的 `views.sync((id, state) => …)` 箭头闭包。
   2. 手写数学在渲染模块间重复：`mergeParts` / `boxPart` / `withShade`（`entityViews.ts` / `sheepModel.ts` / `arenaArt.ts` 各一份）、`hashFloat`（`particles.ts`）、`clamp01` 与指数平滑（`emblem.ts`）。
   3. 魔数未集中：`MATERIAL_BUDGET` 在 `render/materials.ts` 与 `ui/debugPanel.ts` 各定义一次；256 粒子上限在 `render/particles.ts`（`PARTICLE_CAPACITY`）与 `ui/debugPanel.ts`（`PARTICLE_BUDGET`）各一份。
   4. `net/sheepVisual.ts` 只能从 `entity.state` 反推羊形，因为快照里没有 `sheepKind` 字段。
   5. 护甲条是死 UI：护甲不在线上协议里，`main.ts` 恒传 `armorRatio: undefined`，HUD 只走 `display: none` 分支。
   6. 倒地灰化只作用于覆盖层的 `backdrop-filter`（`styles.css` 的 `.hud-downed`：`saturate(0.25) grayscale(0.4)`），并没有真的给 3D 场景去色。
   7. `settings/store.ts` 把 `sfxVolume` 加进了冻结的 `ac.settings.v1` 结构，但没有配套修订 P04 文档（P04 §5.5 的字段表仍只有 sensitivity/fov/masterVolume/colorblindSafe/reduceMotion 五项）。
   8. 色盲安全模式只切换调色板，没有为危险度提供"轮廓粗细"这第二视觉通道（§5.4 承诺的该项未落地）。

## 6. 验证

| # | 命令 | 期望输出 | 失败意味着 |
|---|---|---|---|
| 1 | `pnpm --filter @ac/client test` | 额标强度、音效惰性初始化、粒子池上限用例通过 | 表现层缺少可回归的部分 |
| 2 | `pnpm check` | 全绿 | — |
| 3 | Edge 手动走 AC-09：把问界羊打到残血并截图对比 | 额标明显更亮；强度单调递增 | FR-09 未达成 |
| 4 | 调试面板读数（40 实体场景） | draw call ≤ 120、帧时间 P95 ≤ 20ms | 性能预算超标，需削减粒子/阴影 |
| 5 | 检查浏览器网络面板 | 除 Vite/HMR 与 WebSocket 外无任何外部请求；无图片/音频文件请求 | 违反零外部素材（NFR-07） |
| 6 | 开启"减少动态"后手动体验 | 屏幕抖动与暗角脉动消失，判定信息不缺失 | 可访问性开关无效（FR-14） |
| 7 | 开启"色盲安全"后截图 | 危险度不依赖红绿差异 | 可访问性设计不达标 |

## 6.1 实测证据（本次执行）

> 全部命令在并行 `main.ts` 接线任务完成、`pnpm check` 恢复全绿后于本地 Windows Git Bash 工作树采集；`git status` / `git diff` 两条在本文档回填后复测。

| # | 命令 | 实测结果 |
|---|---|---|
| 1 | `pnpm check` | 全绿：`tsc --noEmit`（shared/server/client）+ `eslint .` + prettier「All matched files use Prettier code style!」+ vitest **55 个测试文件 / 343 个用例全部通过** + `check-docs` OK |
| 2 | `pnpm exec vitest run --project client` | **28 个测试文件 / 153 个用例通过**，耗时 2.21s |
| 3 | `pnpm --filter @ac/client run build` | **84 modules transformed**，耗时 240ms；`dist/index.html` 0.76 kB（gzip 0.39 kB）、`dist/assets/index-fYIltKuJ.css` 12.94 kB（gzip 3.53 kB）、`dist/assets/index-d4cQSTYj.js` **661.00 kB（gzip 176.63 kB）**；有一条 >500 kB chunk 警告 |
| 4 | `node tools/check-docs.mjs` | 扫描 22 个文档、57 条相对链接；10 份计划 + 11 份前置文档，线性链与链接校验通过 |
| 5 | `git status --short` | 36 行：14 个已跟踪文件改动/删除 + 22 个未跟踪路径（新增 `render/**`、`audio/**`、相关测试与 `net/sheepVisual.ts` 等；含本次回填的本文档） |
| 6 | `git diff M7 --stat` | 14 个文件，**1660 插入 / 856 删除**（含本次回填的本文档 +57 / −12；其余 P09 源码为 13 个文件 / 1603 插入 / 844 删除） |
| 7 | `ls packages/client/src/render packages/client/src/audio` | render 19 个文件（其中 5 个 `*.test.ts`）；audio 11 个文件（其中 5 个 `*.test.ts`） |

## 7. DoD（验收标准）

1. `pnpm check` 全绿；额标相关单测存在且通过。
2. AC-09 通过：`emblemIntensity` 规则可视化生效，附截图对比。
3. §5.3 的性能预算全部达标（调试面板读数）。
4. 零外部素材：网络面板无外部资源请求；仓库无二进制资源文件。
5. 三把武器的音效与视模型动画可被主观区分；狂暴激活有明确的视听反馈。
6. 可访问性两项开关生效并持久化。

## 7.1 DoD 状态

| # | DoD 项 | 状态 | 证据 |
|---|---|---|---|
| 1 | `pnpm check` 全绿；额标相关单测存在且通过 | **通过** | §6.1 #1：55 文件 / 343 用例全绿；`render/emblem.test.ts`、`render/materials.test.ts`、`test/entityViewsFlags.test.ts` 覆盖额标纹理、强度规则、着色器补丁与镜像标志位 |
| 2 | AC-09 通过：`emblemIntensity` 规则可视化生效，附截图对比 | **未执行** | 强度规则单测通过（满血 0.2 / 空血 1.0 / 单调递增），但"打到残血截图对比"是人工浏览器步骤（§6 #3），本次未做 |
| 3 | §5.3 性能预算全部达标（调试面板读数） | **部分通过** | `test/budget.test.ts` 在 40 实体静态场景断言 draw call / 三角形 / 材质 ≤ 预算并通过；真实浏览器 draw call 与帧时间 P95 读数（§6 #4）**未执行** |
| 4 | 零外部素材：网络面板无外部资源请求；仓库无二进制资源文件 | **未执行** | 仓库侧静态证据通过（`git diff M7 -- package.json pnpm-lock.yaml packages/client/package.json` 为空、无新增二进制素材）；**网络面板检查（§6 #5）未执行** |
| 5 | 三把武器的音效与视模型动画可被主观区分；狂暴激活有明确的视听反馈 | **未执行** | 配方与状态机有单测（`audio/sfx.test.ts`、`render/viewmodelModel.test.ts` 通过），但主观音画区分与狂暴反馈需在浏览器中人工体验，本次未做 |
| 6 | 可访问性两项开关生效并持久化 | **未执行** | `ui/settings.test.ts`、`settings/store.test.ts` 覆盖开关写回 `ac.settings.v1`；"减少动态 / 色盲安全"的视觉生效检查（§6 #6、#7）为人工浏览器步骤，本次未做 |

## 8. 风险与回滚

| 风险 | 触发信号 | 对策 |
|---|---|---|
| 程序化美术观感不佳（R-04） | 内部观感清单不通过 | 优先加强轮廓光、雾、配色对比与特效，而非增加多边形；必要时只替换 `materials.ts` 调色板 |
| 粒子与音效拖累帧率 | 帧时间 P95 > 20ms | 粒子并发降到 128、发声上限降到 16；先减特效再加内容 |
| `OffscreenCanvas` 兼容性 | 某些浏览器不支持 | 已设计回退到普通 `canvas`；单测覆盖两条路径 |
| 音效吵闹/刺耳 | 主观反馈 | 所有配方集中在 `sfx.ts`，音量与滤波参数可独立调；提供 sfx 总线音量滑块 |
| 额标在小屏上不可读 | 远距离看不出额标 | 给 `elite`/`king` 的额标加轻微描边光晕，并让额标尺寸随羊形变量（§5.1 已按羊形区分） |

**回滚目标**：`HANDOFF-H08`。移除 `render/{materials,emblem,sheepModel,viewmodelModel,particles,effects,arenaArt}.ts` 与 `audio/**`，退回灰盒渲染（玩法与网络完全不受影响）。

## 9. 移交物

**移交物 ID**：HANDOFF-H09

稳定接口与能力清单：

1. 额标规范与 `emblemIntensity` 规则（P10 的验收报告会引用它作为 AC-09 证据）。
2. 程序化美术资产模块（材质、羊模型、视模型、粒子、场景美术）与调色板/色盲变体。
3. WebAudio 合成层（`synth`/`sfx`/`mixer`）与音量总线。
4. HUD 与状态呈现规则表、设置面板、可访问性开关。
5. 性能预算表（draw call / 三角面 / 粒子 / 发声 / 帧时间）。
6. 已验证能力：AC-09 通过；性能预算达标；零外部素材；可访问性开关生效。
