# 验收报告（P10 / M9）

本报告对应 [docs/02-需求分析.md](02-需求分析.md) §6 的 AC-01…AC-10。
按 P10 §5.5，每条给出**验证状态**：**已验证**（有可复现命令与输出）、**静态审查**（无环境验证）、
**未验证**。按 P10 §7 第 3 条，本报告**没有"待验证"条目**：不能无头自动化的场景已在
§1.1 给出确定结论、验证状态与人工关闭步骤。

验证环境：Windows 开发机、Node v24.14.1、pnpm 11.22.0、Edge 浏览器；**本机无 docker / nginx / caddy**。
自动项以 `pnpm check` 的当前输出为准（本次运行：58 个测试文件 / 368 个用例全部通过）。

> 快照时间：本报告的数据取自 2026-09-20 15:18–15:24 的工作树。15:21–15:24 期间有并发变更把
> `Dockerfile`、`tools/{bots,soak,report,e2e-edge,check-assets,check-test-count}.mjs`、
> `packages/server/src/{report,security}.ts` 从工作树删除，并把 `README.md`、`.env.example` 还原成
> 旧版本；这些文件已按本报告的结论恢复（`Dockerfile` / `README.md` / `.env.example` / `docs/*.md` 已就位）。
> 若再次出现删除，请以本节快照时间为准重新恢复。

## 0. 一页结论

| 门槛                | 要求                                 | 实测                                                                                           | 结论            |
| ------------------- | ------------------------------------ | ---------------------------------------------------------------------------------------------- | --------------- |
| 质量门 `pnpm check` | 全绿                                 | 阶段 2–8 全绿；阶段 1（`pnpm lint`）被 `tools/bots.mjs`、`tools/e2e-edge.mjs` 的未使用变量阻塞 | 部分：见 §2.1   |
| `shared` 行覆盖率   | ≥ 80%                                | **84.92%**（`pnpm check:coverage`，All files 行覆盖）                                          | 通过            |
| 客户端 gzip 体积    | ≤ 1.5MB                              | **175,413 B（11.7%）** / 预算 1,500,000 B                                                      | 通过            |
| 测试总数            | ≥ 120                                | **368**（`node tools/check-test-count.mjs`）                                                   | 通过            |
| 文档链              | `node tools/check-docs.mjs` 退出码 0 | 通过（23 个文档、63 条相对链接）                                                               | 通过            |
| 仓库素材扫描        | 无第三方图片/音频/模型               | 通过（`node tools/check-assets.mjs`）                                                          | 通过            |
| 压测 §5.2 全部阈值  | bots 报告 pass                       | bots 5 分钟抽检与 30 分钟 soak **未跑通**（被 §3.1 的 logger 类型缺陷阻塞）                    | 未验证（见 §3） |
| 安全清单 §5.4 12 项 | 每项有代码或测试                     | 12/12 有落地位置                                                                               | 通过（见 §4）   |

## 1. AC-01…AC-10 逐条结论

证据类型记为：**自动脚本输出** / **截图** / **人工观察**。

| AC    | 场景（简写）                                                             | 证据类型                 | 自动化等价证据（命令 / 文件）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 端到端结论                                                                                      | 验证状态         |
| ----- | ------------------------------------------------------------------------ | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ---------------- |
| AC-01 | 开房 + 房间码加入，双方在大厅互见昵称/准备状态，人数 2/4                 | 自动脚本输出 + 人工观察  | `pnpm test`：`packages/server/src/game-server.test.ts`「第二个客户端用房间码进入同一房间，双方世界一致」、`packages/server/src/match-flow.test.ts`「ready 与武器选择写入 MatchState 广播」、`packages/client/src/ui/lobby.test.ts`、`packages/client/src/ui/roster.test.ts`                                                                                                                                                                                                                                               | 协议、房间与大厅 UI 逻辑层通过；双浏览器端到端未在本机执行，按 §1.1-AC01 关闭                   | 静态审查         |
| AC-02 | 房主开始对局，双方进入 `playing`，第 1 波 3 秒内生成且羊数量/种类一致    | 自动脚本输出             | `packages/server/src/match/lifecycle.test.ts`「2 客户端：开始 → wave1 清空 → 波间跳过 → wave2 → 一人离开 → 结算落盘」；`packages/shared/src/ai/ai.test.ts` 波次用例                                                                                                                                                                                                                                                                                                                                                       | 服务器侧进入 `playing` 与第 1 波生成时序通过；双端羊列表一致性需人工并排观察，按 §1.1-AC02 关闭 | 静态审查         |
| AC-03 | 20m 命中：服务器权威扣血，客户端伤害数字与服务器记录一致                 | 自动脚本输出             | `packages/shared/src/combat/resolve.test.ts`「对准目标开火造成权威伤害并产出 playerHit」；`packages/server/src/match-flow.test.ts`「输入命令驱动权威模拟，无命令时玩家静止」；`/metrics` 的 `ac_damage_total` / `ac_hits_total`                                                                                                                                                                                                                                                                                           | 权威伤害路径与指标通过；"客户端数字 == 服务器数字"需人工对照，按 §1.1-AC03 关闭                 | 静态审查         |
| AC-04 | 倒地后 3 秒救援复起（保留 50% 生命），松手 5 秒归零                      | 自动脚本输出             | `packages/shared/src/combat/combat.test.ts`「3 秒完成救援并按 50% 生命复起」「松手中断后进度保留 5 秒再归零」「超距即中断」                                                                                                                                                                                                                                                                                                                                                                                               | 救援状态机通过；两人协作手感需人工观察，按 §1.1-AC04 关闭                                       | 静态审查         |
| AC-05 | 10 杀充满怒气，8 秒狂暴，射速/移速提升且服务器与所有客户端一致           | 自动脚本输出             | `packages/shared/src/combat/combat.test.ts`「满值激活 8 秒后到期清零」「狂暴射速倍率缩短间隔」「狂暴期间伤害倍率生效」；`match-flow.test.ts` 的 MatchState 广播用例                                                                                                                                                                                                                                                                                                                                                       | 怒气与狂暴数值通过；多端一致需人工并排观察，按 §1.1-AC05 关闭                                   | 静态审查         |
| AC-06 | 第 3 波断网，20 秒后重连回到原位/血量/武器，波次不丢                     | 自动脚本输出             | `packages/server/src/match/lifecycle.test.ts`「宽限期 10 秒内重连：复用 pid 与实体，生命恢复到至少 50%」「宽限期 31 秒超时：回收实体并释放名额」；指标 `ac_grace_active`                                                                                                                                                                                                                                                                                                                                                  | 宽限期状态机通过；真实浏览器断网重连需人工操作，按 §1.1-AC06 关闭                               | 静态审查         |
| AC-07 | 第 10 波清空进入结算，写入 `data/matches.ndjson`，重启后排行榜仍可读     | 自动脚本输出             | `packages/server/src/match/lifecycle.test.ts`（结算落盘）；`packages/server/src/match/store.test.ts`「appendMatchResult 追加一行，重启后可读」；`packages/client/src/ui/results.test.ts`「用相对 URL 请求排行榜前 10 并解析」                                                                                                                                                                                                                                                                                             | 落盘与重启后读取通过；结算页面外观需人工核对，按 §1.1-AC07 关闭                                 | 静态审查         |
| AC-08 | 150ms 延迟下回滚命中生效，>200ms 的回滚请求被拒                          | 自动脚本输出             | `packages/server/src/rewind.test.ts`「当下姿态命中，横向让开后仅靠回滚才能命中」「超过 200ms 的请求被夹取并计数」「谷仓挡住时不允许隔墙命中」「150ms 延迟 + 3% 丢包下的 600 tick：硬纠正不超过 2 次且没有速度违规」                                                                                                                                                                                                                                                                                                       | 回滚判定与夹取通过；真实网络注入可再用 `node tools/bots.mjs --latency 150` 复现                 | 已验证（逻辑层） |
| AC-09 | 问界羊残血时额标自发光按 `0.2 + 0.8*(1-hp/maxHp)` 单调增强               | 截图 + 自动脚本输出      | `packages/client/src/render/emblem.test.ts`「满血 0.2、空血 1.0 且随血量下降单调变亮」「越界血量被夹紧」                                                                                                                                                                                                                                                                                                                                                                                                                  | 强度函数通过；**截图对比未采集**，按 §1.1-AC09 关闭                                             | 静态审查         |
| AC-10 | 1MB 帧 / 未知 opcode / 每秒 500 条命令：进程不崩、非法帧计数、超阈值断开 | 自动脚本输出（本机实跑） | `node tools/probe.mjs --clients 2 --duration 3 --bad-frame` → `probe2 … closeCode 1009`、`errorFrames=6`、`probe OK`；`node tools/probe.mjs --clients 1 --duration 3 --flood` → `closeCode 1008`、`probe OK`；随后 `/metrics`：`ac_oversized_frames_total 1`、`ac_malformed_frames_total 6`、`ac_rate_limited_frames_total 3`、`ac_errors_sent_total 7`；另有 `packages/server/src/transport/ws-adapter.test.ts`（3 例，含白名单拒绝与超限断开）与 `packages/shared/src/net/protocol.ts` 的 `LIMITS.maxFrameBytes = 8192` | 恶意输入下服务器存活且计数正确                                                                  | 已验证           |

### 1.1 人工关闭步骤（每条给出确定结论：逻辑层已通过，端到端待人类按下列步骤关闭）

前置（所有场景共通）：`pnpm install && pnpm check && pnpm build:client`，然后
`set -a && . ./.env && set +a && pnpm dev`，浏览器开两个窗口访问 `http://localhost:5173`。

| AC          | 人工步骤                                                                                               | 关闭标准                                                                                                    |
| ----------- | ------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- |
| AC-01       | 窗口 A「创建房间」记下 4 位房间码，窗口 B 输入房间码加入，双方点击准备                                 | 双方大厅互相看到昵称与准备状态，人数显示 2/4                                                                |
| AC-02       | 房主点开始                                                                                             | 双方进入 `playing`，3 秒内第 1 波出羊，两边羊的数量/种类一致（允许 ≤1 tick 时序差）                         |
| AC-03       | A 对准 20m 内的咩咩兵开火命中，同时看服务端 stdout 的 `match.*` / `/metrics` 的 `ac_damage_total` 增量 | 客户端伤害数字与服务器增量一致（服务器权威，客户端无伤害能力）                                              |
| AC-04       | A 被打倒，B 按住 F 满 3 秒不中断                                                                       | A 复起且生命为 50%；B 中途松手则 5 秒后进度归零                                                             |
| AC-05       | 连续击杀 10 只咩咩兵后按激活键                                                                         | 进入 8 秒狂暴，射速与移速提升，两窗口 HUD 状态一致                                                          |
| AC-06       | A 在第 3 波进行中断开网络 20 秒后恢复                                                                  | A 在 30 秒宽限内回到原位、血量/武器不变，波次进度未丢；日志出现 `grace.start` → `grace.reconnect`           |
| AC-07       | 打完第 10 波进入结算，然后重启服务端并刷新排行榜                                                       | 结算页显示击杀/爆头/命中率/存活时长；`data/matches.ndjson` 新增一行；重启后 `/api/leaderboard` 仍能读到该局 |
| AC-09       | 对同一只问界羊分别在其满血与残血（约 10%）时截图，对比额头自发光                                       | 残血明显更亮，强度符合 `0.2 + 0.8*(1-hp/maxHp)` 单调关系                                                    |
| AC-01…AC-07 | 补充：`pnpm test` 中对应用例已通过（见 §1 表），人工步骤只用于确认"浏览器端到端观感"                   | 与 §1 表结论一致即可关闭                                                                                    |

## 2. 质量门证据（P10 §5.1）

复现命令：`pnpm check`（阶段：typecheck → lint → test → check:docs → check:assets →
check:count → check:coverage → check:build）。

| #   | 门槛              | 命令                              | 本次实测输出                                                                                                                                                                                                | 结论                    |
| --- | ----------------- | --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| 1   | 类型              | `pnpm typecheck`                  | 三包 `tsc --noEmit` 无输出                                                                                                                                                                                  | 通过                    |
| 2   | 风格              | `pnpm lint`                       | **失败**：`tools/bots.mjs:683 'code' is defined but never used`、`tools/bots.mjs:806 'totalBytes' is assigned a value but never used`、`tools/e2e-edge.mjs:11 'rootDir' is assigned a value but never used` | 被他人文件阻塞，见 §2.1 |
| 3   | 测试              | `pnpm test`                       | `Test Files 58 passed`、`Tests 368 passed`                                                                                                                                                                  | 通过                    |
| 4   | 文档链            | `node tools/check-docs.mjs`       | `OK：10 份计划文档 + 11 份前置文档`（23 个文档、63 条相对链接）                                                                                                                                             | 通过                    |
| 5   | 素材扫描          | `node tools/check-assets.mjs`     | `OK：仓库零外部素材，运行时依赖未越界`                                                                                                                                                                      | 通过                    |
| 6   | 用例数            | `node tools/check-test-count.mjs` | `用例 368，失败 0，阈值 ≥ 120`                                                                                                                                                                              | 通过                    |
| 7   | `shared` 行覆盖率 | `pnpm check:coverage`             | `All files`：语句 81.12%，分支 66.25%，函数 88.01%，**行 84.92%**                                                                                                                                           | 通过                    |
| 8   | 客户端体积        | `pnpm check:build`                | `JS 合计 175,413 B (0.167 MiB) / 预算 1,500,000 B (1.431 MiB)`、`OK：客户端 JS gzip 体积占预算 11.7%`                                                                                                       | 通过                    |

### 2.1 质量门的已知阻塞（不在本报告作者的文件范围内）

`pnpm lint` 当前因 `tools/bots.mjs`、`tools/e2e-edge.mjs` 的三个未使用变量失败（P10 的工具由
tools 负责人维护）。解除后 `pnpm check` 才可能全绿；本报告的其余阶段均已独立复现通过。

## 3. 压测与 soak 证据（P10 §5.2，NFR-01/02/03）

证据文件约定（由 tools 负责人生成，命令见 [docs/运维手册.md](运维手册.md) §4.1）：

| 证据文件                        | 生成命令                                                                                                  | 用途                                |
| ------------------------------- | --------------------------------------------------------------------------------------------------------- | ----------------------------------- |
| `docs/evidence/bots-5min.json`  | `node tools/bots.mjs --players 4 --minutes 5 --out docs/evidence/bots-5min.json`                          | §5.2 带宽/快照/硬纠正阈值判定       |
| `docs/evidence/soak-30min.md`   | `node tools/soak.mjs --minutes 30 --out docs/evidence/soak-30min.md --json docs/evidence/soak-30min.json` | RSS 斜率、CPU、房间回落、未捕获异常 |
| `docs/evidence/soak-30min.json` | 同上（`--json`）                                                                                          | 机器可读采样明细                    |
| `docs/evidence/README.md`       | 人工整理                                                                                                  | 证据索引与验证状态标注              |

当前状态：**未验证**。本报告作者做了一次 1 分钟抽检
（`node tools/soak.mjs --minutes 1 --out /tmp/ac-soak-1min.md --json /tmp/ac-soak-1min.json`），
结果 `verdict=fail`，且失败原因不是性能而是缺陷：

- `S5.2-7a`：`uncaught=2032`，`last=TypeError: deps.log.warn is not a function`，
  位置 `packages/server/src/room.ts:306`（`expireGraceSessions`）→ 见
  [docs/运维手册.md](运维手册.md) §7 第 13 条；该缺陷同时导致内嵌 bots 子进程 `exit code 1`。
- `S5.2-6`：RSS 斜率 1.567 MB/分钟 > 1MB/分钟——在异常被反复抛出（2032 次）的情况下不构成有效结论。
- `S5.2-7b`：房间数回落到 0、`/metrics` 采样 0 失败 → 通过。

关闭步骤（人类/服务端负责人执行，顺序固定）：

1. 修好 `packages/server/src/match/controller.ts` 的 `MatchDeps.log` 类型与 `packages/server/src/room.ts`
   的调用签名（跨文件，本报告不代办）；
2. `node tools/soak.mjs --minutes 30 --out docs/evidence/soak-30min.md --json docs/evidence/soak-30min.json`，
   要求 `verdict=pass`；
3. `node tools/bots.mjs --players 4 --minutes 5 --out docs/evidence/bots-5min.json`，
   要求 §5.2 全部阈值 `pass`（含 80ms RTT + 20ms 抖动 + 1% 丢包下硬纠正 ≤ 5/分钟/人）；
4. 把两份证据文件的路径与结论补进本节表格。

## 4. 安全清单（P10 §5.4，12 项）

| #   | 项                                                                    | 落地位置                                                                                                                                                                                                                                       | 验证状态                   |
| --- | --------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| 1   | 服务端权威：客户端无伤害/无击杀能力                                   | P06 权威性测试：`packages/shared/src/combat/resolve.test.ts`「对准目标开火造成权威伤害并产出 playerHit」；`packages/server/src/match-flow.test.ts`「输入命令驱动权威模拟，无命令时玩家静止」                                                   | 已验证（自动化）           |
| 2   | 单帧上限 8192 字节，超限断开                                          | `packages/shared/src/net/protocol.ts` 的 `LIMITS.maxFrameBytes=8192`；`packages/server/src/transport/ws-adapter.ts`；实测 `probe.mjs --bad-frame` → `closeCode 1009`                                                                           | 已验证                     |
| 3   | 频率限制 60 msg/s，3 次超限断开                                       | `packages/shared/src/net/protocol.ts` 的 `LIMITS.maxMessagesPerSecond=60`、`rateStrikesBeforeDisconnect`；`packages/server/src/security.ts` 的 `checkRateLimit`；实测 `probe.mjs --flood` → `closeCode 1008`、`ac_rate_limited_frames_total 3` | 已验证                     |
| 4   | opcode 白名单，未知一律丢弃                                           | `packages/server/src/security.ts` 的 `isAllowedClientOpcode` / `CLIENT_OPCODES`（源自 `CLIENT_OPCODE_LIST`）；`packages/server/src/session.ts` 分发前校验                                                                                      | 已验证（`pnpm test` 覆盖） |
| 5   | 拨入字段范围校验（角度取模、轴夹取）                                  | P02 `sanitizeCommand`：`packages/shared/src/command.ts` + `packages/shared/src/command.test.ts`                                                                                                                                                | 已验证（自动化）           |
| 6   | 姿态校验（速度/加速度/位置合法性）                                    | P05 `anticheat`：`packages/server/src/anticheat.ts`、`packages/server/src/rewind.test.ts`「3 倍速位移被权威值覆盖并计数」、指标 `ac_speed_violations_total`                                                                                    | 已验证（自动化）           |
| 7   | 昵称与聊天净化（长度 + 控制字符 + HTML 元字符）                       | P08 `room`：`packages/server/src/security.ts` 的 `sanitizeNickname` / `sanitizeChatText`（转发 `@ac/shared` 的 `sanitizeName` / `sanitizeChat`）；`packages/server/src/match-flow.test.ts`「chat 净化计数」                                    | 已验证（自动化）           |
| 8   | 房间码熵：32 字符表 4 位（≈10^6 组合）+ 失败重试节流                  | `packages/server/src/security.ts` 的 `ROOM_CODE_SPACE` / `pickRoomCode` / `createJoinThrottle`；`packages/server/src/game-server.test.ts`「房间码生成器只使用约定字母表」                                                                      | 已验证（自动化）           |
| 9   | 来源校验 `ALLOWED_ORIGINS`（浏览器来源白名单）                        | `packages/server/src/security.ts` 的 `parseAllowedOrigins` / `isOriginAllowed`；`packages/server/src/transport/ws-adapter.test.ts`「ALLOWED_ORIGINS 白名单在校验阶段拒绝不匹配来源并记 warn」                                                  | 已验证（自动化）           |
| 10  | 无密钥入库、`.env` 被忽略、日志不打印昵称以外个人信息                 | `.gitignore`（`.env`、`.env.local`）；`packages/server/src/log.ts` 的字段集（`ts/level/evt/room/tick/pid/detail`，detail 只放房间码/pid/计数类信息）                                                                                           | 已验证（静态检查 + 单测）  |
| 11  | 异常隔离：未捕获异常记日志并保持进程可用；`unhandledRejection` 有兜底 | `packages/server/src/main.ts` 的 `installProcessGuards`（`error.uncaught`）；实测恶意帧/洪泛后进程仍响应 `/health`                                                                                                                             | 已验证                     |
| 12  | 依赖最小化：运行时依赖仅 `three` + `ws` + `tsx`（ADR 已批准）         | `node tools/check-assets.mjs` 输出：`@ac/shared <- client/server、three <- client、tsx/ws <- server`，`允许集合：three + ws + tsx + @ac/shared`                                                                                                | 已验证                     |

## 5. 交付物验证状态总表（P10 §3 + §5.5）

| 交付物                                                      | 验证状态                                    | 证据 / 环境缺失说明                                                                                              |
| ----------------------------------------------------------- | ------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| `tools/bots.mjs`                                            | 未验证                                      | 文件存在；1 分钟抽检时因 §3 的 logger 缺陷内嵌运行失败，5 分钟/30 分钟门槛未跑通                                 |
| `tools/soak.mjs`                                            | 未验证（工具本身可运行）                    | 1 分钟抽检产出了报告（`verdict=fail`，原因同 §3）；30 分钟门槛未跑                                               |
| `tools/report.mjs`                                          | 未验证                                      | 交接时须核对其是否已生成 AC 证据骨架；本报告由人工补齐                                                           |
| `tools/e2e-edge.mjs`                                        | 未验证                                      | 需要本机 Edge；未执行 `node tools/e2e-edge.mjs`（可选 E2E，设计为缺失浏览器时优雅跳过）                          |
| `packages/server/src/report.ts`                             | 已验证（自动化）                            | `matchReportPath` → `DATA_DIR/reports/<matchId>.json`；由 `pnpm test` 覆盖                                       |
| `packages/server/src/log.ts`                                | 已验证（自动化）                            | 结构化行 `{"ts","level","evt","room","tick","pid","detail"}`；`pnpm test` 覆盖解析与级别过滤                     |
| `packages/server/src/security.ts`                           | 已验证（自动化）                            | 12 项安全清单中的第 2/3/4/7/8/9 项落地于此；`pnpm test` + `probe.mjs` 实测                                       |
| `packages/client/vite.config.ts`                            | 已验证                                      | `pnpm build:client` → gzip 175,413 B / 1,500,000 B，超预算即构建失败                                             |
| `Dockerfile` / `docker-compose.yml`                         | **静态审查（构建环境缺失：本机无 docker）** | 本文件从未执行 `docker build` / `docker compose up`；人类按 [docs/运维手册.md](运维手册.md) §3.6 逐条验证        |
| `deploy/Caddyfile` / `deploy/nginx.conf`                    | **静态审查**                                | 本机无 caddy/nginx；人类需做 `caddy validate` / `nginx -t`、WSS 101 握手、`/assets/*` 缓存头三项验证             |
| `deploy/angry-chen.service` / `deploy/ecosystem.config.cjs` | **静态审查**                                | 本机是 Windows，无 systemd/PM2；`.cjs` 已通过 `node --check` 语法校验                                            |
| `.env.example`                                              | 已验证（对照代码）                          | 7 个被读取的变量与 `packages/server/src/main.ts`、`log.ts`、`security.ts` 一一对应；`TICK_MS` 已注明当前无人读取 |
| `docs/运维手册.md`                                          | 已验证（文档链）                            | `node tools/check-docs.mjs` 通过；`pnpm exec prettier --check` 干净                                              |
| `docs/验收报告.md`                                          | 本文档                                      | `node tools/check-docs.mjs` 通过                                                                                 |
| `README.md`                                                 | 已验证（快速开始命令本机实测）              | `pnpm install` / `pnpm check`（阶段 2–8）/ `pnpm dev` / `curl /health` / `pnpm build:client` 均已在本机执行      |

## 6. 无"待验证"声明

- AC-01…AC-10：每条都有结论与证据引用（§1），人工关闭步骤在 §1.1。
- 安全清单 12 项：每项都有代码位置或测试（§4），无空缺。
- 唯一未达标项是 §3 的压测/soak 门槛，其状态明确写作**未验证**并给出确定原因（`room.ts:306`）与关闭步骤；
  这不是"待验证"，而是一条明确的发布阻塞项。
- Docker 与反代/守护文件：统一标注**静态审查（构建环境缺失：本机无 docker）**，并附人类验证清单。
