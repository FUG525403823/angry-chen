# 复制为 .env 后按需修改。变量全集来自 ADR-004（docs/00-共识/ADR/ADR-004-部署与持久化形态.md）
# 与 ADR-007（docs/00-共识/ADR/ADR-007-单进程静态托管与同源寻址.md）；仓库不提交任何真实密钥。
#
# .env 的加载（ADR-007）：CLI 入口（packages/server/src/main.ts 的 main()）用 Node 原生
# process.loadEnvFile 依次尝试「当前工作目录/.env」与「仓库根/.env」；没有 .env 属正常情况。
# 已经存在的真实环境变量优先级更高（Node 原生语义）。用 AC_ENV_FILE=/abs/path.env 指定其它路径。
# 各变量的真实读取点以注释里的文件为准（可用 grep 自证）。

# ---- 监听（读取位置：packages/server/src/main.ts 的 startServer）----
# 单进程同时承载 HTTP 与 WebSocket 的端口；默认 8787（本机 8080 常被其它软件占用）。
PORT=8787
# 绑定地址：模板默认 0.0.0.0 = 同一局域网/公网的朋友可以直接连（启动日志的 joinUrls 就是可分享地址，ADR-007）。
# 代价：这份 .env 生效期间本机 8787 端口对外可达。只在本机开发请改回 127.0.0.1；
# 公网直连请用云安全组/防火墙限定来源，或改用 deploy/ 的可选反代样例。
HOST=0.0.0.0

# ---- 客户端静态资源（读取位置：packages/server/src/main.ts → static.ts）----
# 不写 = 自动探测 <仓库根>/packages/client/dist（先跑 pnpm build:client，或直接用 pnpm serve）；
# off/none = 关闭静态托管，退回「只跑 /health、/metrics、/api/* 与 WebSocket」的旧行为。
# 相对路径按进程 cwd 解析（pnpm --filter 的 cwd 是 packages/server，建议写绝对路径）。
# CLIENT_DIST=./packages/client/dist

# ---- 房间容量（读取位置：packages/server/src/main.ts，缺省回落到 @ac/shared 的 LIMITS）----
# 进程内房间数上限；上调前先确认内存与单核预算。
MAX_ROOMS=64
# 单房间人数上限（玩法为 1–4 人协作）。
MAX_PLAYERS_PER_ROOM=4

# ---- 模拟调度（ADR-004 §3：只影响服务器调度，不改模拟步长）----
# 服务器调度步长（毫秒）—— 未实现：当前代码不读取该变量，勿依赖（O10 卫生项）。
# 实际步长来自 packages/shared/src/index.ts 的 SERVER_TICK_MS = 50。
# TICK_MS=50

# ---- 持久化（读取位置：packages/server/src/main.ts → packages/server/src/match/store.ts）----
# 对战记录 matches.ndjson 与诊断报告 reports/<matchId>.json 的落盘目录；生产务必用绝对路径；
# 删除该目录即重置本地战绩。注意 pnpm --filter 的 cwd 是 packages/server，相对路径会落在包目录下。
DATA_DIR=./data
# 常驻内存的战绩上限（超出按 startedAtMs 最旧淘汰；NDJSON 文件不截断，历史仍可离线分析）。默认 10000。
MATCH_STORE_MAX_RECORDS=10000
# reports/ 诊断报告保留份数（按 mtime 保留最近 N 份）。默认 200；0 = 不清理。
REPORT_RETENTION=200

# ---- 日志（读取位置：packages/server/src/log.ts 的 createStdoutLogger / resolveLogLevel）----
# 结构化 JSON 行日志级别：debug | info | warn | error；行结构 ts/level/evt/room/tick/pid/detail。
LOG_LEVEL=info

# ---- 来源校验（读取位置：packages/server/src/security.ts 的 parseAllowedOrigins / createOriginGuard）----
# 逗号分隔的浏览器 Origin 白名单；不设置（或 *）= 不校验。
# 单进程同源托管（ADR-007）时不要设置：页面与 WebSocket 同源，没有跨源请求。
# 只有「页面与游戏服务器不同源」或走反代时才需要，例如 ALLOWED_ORIGINS=https://game.example.com；
# 照抄 http://localhost:5173 会让外部玩家被 security.origin_rejected 拒绝。
# ALLOWED_ORIGINS=http://localhost:5173
